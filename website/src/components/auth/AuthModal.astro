---
import type { AccountContent } from '../../i18n/schema'

const { copy, skinClass = '', idPrefix = 'download' } = Astro.props as {
  copy: AccountContent
  skinClass?: string
  idPrefix?: string
}
const authModalClass = ['download-auth-modal', skinClass].filter(Boolean).join(' ')
const titleId = `${idPrefix}-auth-title`
---

<div class={authModalClass} data-download-auth-modal hidden>
  <button
    type="button"
    class="download-auth-backdrop"
    data-download-auth-close
    aria-label={copy.auth.closeLabel ?? 'Close'}
  ></button>

  <div class="download-auth-dialog" role="dialog" aria-modal="true" aria-labelledby={titleId}>
    <div class="download-auth-heading">
      <p class="download-auth-eyebrow">{copy.auth.eyebrow}</p>
      <h2 id={titleId}>{copy.auth.modalTitle ?? copy.auth.title}</h2>
      {copy.auth.modalDescription && <p>{copy.auth.modalDescription}</p>}
    </div>

    <button
      type="button"
      class="download-auth-close"
      data-download-auth-close
      aria-label={copy.auth.closeLabel ?? 'Close'}
    >
      <span aria-hidden="true">×</span>
    </button>

    <div
      class="download-google-button"
      data-download-google-login
      aria-label={copy.auth.continueWithGoogle}
    ></div>

    <form class="download-auth-form" data-download-auth-form hidden>
      <label class="download-field">
        <span class="download-field-label">{copy.auth.emailLabel}</span>
        <input
          type="email"
          autocomplete="email"
          class="download-input"
          placeholder={copy.auth.emailPlaceholder}
          data-download-login-email
        />
      </label>

      <button
        type="button"
        class="download-secondary-button download-continue-email-button"
        data-download-continue-email
      >
        {copy.auth.continueWithEmail}
      </button>

      <div class="download-email-code-area" data-download-email-code-area hidden>
        <label class="download-field">
          <span class="download-field-label">{copy.auth.codeLabel}</span>
          <div class="download-code-row">
            <input
              type="text"
              inputmode="numeric"
              autocomplete="one-time-code"
              class="download-input download-code-input"
              placeholder={copy.auth.codePlaceholder}
              data-download-login-code
            />
            <button
              type="button"
              class="download-secondary-button download-send-code-button"
              data-download-send-code
            >
              {copy.auth.sendAgain}
            </button>
          </div>
        </label>

        <button type="submit" class="download-primary-button" data-download-login-submit hidden>
          {copy.auth.signIn}
        </button>
      </div>

      <p
        class="download-inline-status"
        data-download-send-code-status
        role="status"
        aria-live="polite"
      ></p>
    </form>

    <div class="download-auth-footer" data-download-email-entry>
      <span>{copy.auth.or}</span>
      <button type="button" class="download-email-link" data-download-email-entry-button>
        {copy.auth.continueWithEmail}
      </button>
    </div>
    <p class="download-auth-terms">
      {copy.auth.termsNotice}
      {' '}
      <a href="/terms">{copy.auth.termsLink}</a>
      <span aria-hidden="true"> · </span>
      <a href="/privacy">{copy.auth.privacyLink}</a>
    </p>
    <p class="download-inline-error" data-download-auth-error role="alert" hidden></p>
  </div>
</div>

<style is:global>
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .download-field {
    display: grid;
    gap: 10px;
  }

  .download-auth-heading {
    display: grid;
    gap: 10px;
    margin-bottom: 22px;
    padding-right: 54px;
  }

  .download-auth-heading h2 {
    margin: 0;
    color: #0f172a;
    font-size: 28px;
    line-height: 1.08;
  }

  .download-auth-heading p {
    margin: 0;
    max-width: 34ch;
    color: #475569;
    font-size: 14px;
    line-height: 1.6;
  }

  .download-auth-eyebrow {
    width: max-content;
    padding: 6px 10px;
    border: 1px solid rgba(37, 99, 235, 0.14);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.62);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.72);
    color: #2563eb !important;
    font-size: 11px !important;
    font-weight: 800;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  .download-auth-form,
  .download-email-code-area {
    display: grid;
    gap: 16px;
  }

  .download-code-row {
    display: flex;
    align-items: stretch;
    gap: 12px;
  }

  .download-field-label {
    color: #64748b;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  .download-input {
    width: 100%;
    min-height: 58px;
    padding: 0 20px;
    border: 1px solid rgba(37, 99, 235, 0.16);
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.72);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.86),
      0 10px 24px rgba(15, 23, 42, 0.05);
    color: #0f172a;
    font-size: 17px;
    backdrop-filter: blur(14px);
  }

  .download-input:focus {
    outline: 3px solid rgba(14, 165, 233, 0.18);
    border-color: rgba(37, 99, 235, 0.42);
    background: rgba(255, 255, 255, 0.9);
  }

  .download-code-input {
    min-width: 0;
  }

  .download-primary-button,
  .download-secondary-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 58px;
    padding: 0 22px;
    border-radius: 18px;
    font-size: 15px;
    font-weight: 800;
    transition: all var(--transition-normal);
  }

  .download-primary-button {
    color: white;
    background: linear-gradient(135deg, #2563eb, #1d4ed8);
    box-shadow: 0 18px 30px rgba(37, 99, 235, 0.24);
  }

  .download-primary-button:hover {
    transform: translateY(-1px);
  }

  .download-secondary-button {
    color: #0f172a;
    background: rgba(255, 255, 255, 0.54);
    border: 1px solid rgba(37, 99, 235, 0.14);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.76);
    backdrop-filter: blur(12px);
  }

  .download-secondary-button:hover {
    border-color: rgba(37, 99, 235, 0.24);
    color: var(--primary);
  }

  .download-continue-email-button {
    color: white;
    background: linear-gradient(135deg, #2563eb, #1d4ed8);
    border-color: rgba(37, 99, 235, 0.88);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.22),
      0 16px 30px rgba(37, 99, 235, 0.22);
  }

  .download-continue-email-button:hover {
    color: white;
    border-color: #1d4ed8;
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.24),
      0 20px 38px rgba(37, 99, 235, 0.28);
  }

  .download-google-button {
    width: 100%;
    min-height: 54px;
    margin-bottom: 18px;
    display: grid;
  }

  .google-oauth-button {
    width: 100%;
    min-height: 54px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    padding: 0 18px;
    border: 1px solid rgba(15, 23, 42, 0.14);
    border-radius: 8px;
    background: #fff;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.92),
      0 12px 24px rgba(15, 23, 42, 0.08);
    color: #0f172a;
    font-size: 15px;
    font-weight: 800;
    transition:
      transform var(--transition-normal),
      border-color var(--transition-normal),
      box-shadow var(--transition-normal);
  }

  .google-oauth-button:hover {
    border-color: rgba(37, 99, 235, 0.28);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.92),
      0 16px 30px rgba(15, 23, 42, 0.12);
    transform: translateY(-1px);
  }

  .google-oauth-button:disabled {
    cursor: wait;
    opacity: 0.72;
    transform: none;
  }

  .google-oauth-button-icon {
    width: 20px;
    height: 20px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex: 0 0 auto;
    line-height: 0;
  }

  .download-primary-button:disabled,
  .download-secondary-button:disabled {
    cursor: not-allowed;
    opacity: 0.64;
    transform: none;
  }

  .download-send-code-button {
    min-height: 54px;
    padding: 0 18px;
    white-space: nowrap;
    flex-shrink: 0;
    width: auto;
  }

  .download-inline-status,
  .download-inline-error,
  .download-auth-terms {
    min-height: 20px;
    font-size: 13px;
  }

  .download-inline-status {
    color: #475569;
  }

  .download-inline-error {
    color: var(--bad);
  }

  .download-auth-terms {
    margin: 0;
    color: #64748b;
    line-height: 1.5;
  }

  .download-auth-terms a {
    color: #0f172a;
    font-weight: 700;
  }

  .download-auth-footer {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 7px;
    margin-top: 18px;
    color: #64748b;
    font-size: 13px;
    line-height: 1.5;
  }

  .download-email-link {
    min-height: 32px;
    padding: 0;
    border: 0;
    background: transparent;
    color: #1d4ed8;
    cursor: pointer;
    font: inherit;
    font-weight: 800;
  }

  .download-email-link:hover {
    color: #0f172a;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .download-auth-modal[hidden],
  .download-auth-form[hidden],
  .download-auth-footer[hidden],
  .download-email-code-area[hidden],
  .download-primary-button[hidden],
  .download-continue-email-button[hidden] {
    display: none;
  }

  .download-auth-modal {
    position: fixed;
    inset: 0;
    z-index: 130;
    display: grid;
    place-items: center;
    padding: 20px;
  }

  .download-auth-backdrop {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(135deg, rgba(15, 23, 42, 0.46), rgba(37, 99, 235, 0.2)),
      rgba(2, 6, 23, 0.24);
    backdrop-filter: blur(12px) saturate(1.12);
  }

  .download-auth-dialog {
    position: relative;
    width: min(520px, 100%);
    padding: 34px 26px 24px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.72);
    border-radius: 30px;
    background:
      linear-gradient(145deg, rgba(255, 255, 255, 0.94), rgba(239, 246, 255, 0.78)),
      rgba(255, 255, 255, 0.72);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.88),
      inset 0 -1px 0 rgba(37, 99, 235, 0.08),
      0 32px 90px rgba(15, 23, 42, 0.28);
    backdrop-filter: blur(28px) saturate(1.24);
  }

  .download-auth-dialog::before {
    content: "";
    position: absolute;
    inset: 0 0 auto;
    height: 120px;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.68), rgba(255, 255, 255, 0));
    pointer-events: none;
  }

  .download-auth-close {
    width: 40px;
    height: 40px;
    position: absolute;
    top: 18px;
    right: 18px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.58);
    border: 1px solid rgba(37, 99, 235, 0.12);
    color: #0f172a;
    font-size: 24px;
    line-height: 1;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.76),
      0 12px 24px rgba(15, 23, 42, 0.08);
    backdrop-filter: blur(12px);
  }

  @media (max-width: 720px) {
    .download-auth-modal {
      padding: 14px;
    }

    .download-auth-dialog {
      padding: 30px 18px 20px;
      border-radius: 26px;
    }

    .download-auth-heading h2 {
      font-size: 24px;
    }

    .download-primary-button,
    .download-secondary-button,
    .download-google-button {
      width: 100%;
    }

    .download-code-row {
      flex-wrap: wrap;
    }

    .download-send-code-button {
      width: 100%;
    }
  }
</style>
