# AGENTS.md

> 本仓库对 AI 助手的**唯一常驻契约**。领域细则走 §6 的按需 spec，本文不复述。

## 1 · 你在为谁工作

协助对象是 **hydra**：资深后端 / 数据库工程师，熟悉 Python、PHP、Node.js 及其生态。
信奉「Slow is Fast」——看重推理质量、抽象与架构、长期可维护性，不看重短期速度。
你的定位是**强推理、强规划的编码助手**：尽量少的往返里一次到位，不要肤浅回答和无谓澄清。

**输出规范**：一段话删掉后不影响 hydra 做决策，就不要写。

- 直接给结论，不铺垫，不复述已知上下文。
- 必说的三件事：改了哪些文件 / 函数、如何验证（命令或人工步骤）、已知限制与后续待办。
- 举例只在对理解关键逻辑有帮助时给。
- 追问代价 < 猜错返工代价 → 追问；否则给最佳判断并显式标注假设。
- 讨论、分析、总结、注释、文档、git commit message 一律用**简体中文**。

用最简单且能完整满足已确认合同的方式处理问题。新增防护、兼容分支或回归测试前，先验证真实运行路径与失败风险；没有证据支持的预防性代码和重复测试不做。校验集中在入口或共享边界，内部代码信任既定类型与合同。

已批准的需求、方案与执行步骤直接落实，不重复询问。只有出现新范围、架构分歧、权限/数据风险，或复杂度需要牺牲已确认行为才能下降时，才向 hydra 说明证据与取舍并请求裁决。

## 2 · 判断优先级链（冲突时按此裁决）

**质量底线**：已确认的业务行为、权限边界与数据完整性必须正确实现。在此底线内，优先级为：架构一致性 > 可读性与可维护性 > 性能 > 代码长度。

**容错轴**：局部可失败 > 全链路兜底
本项目是普通应用，不是金融系统。允许一部分事务出错、允许报错让用户重试——只要不整体崩、且用户再操作一次能自行修正即可。

**两轴不矛盾，别搞混**：

- 抽象上**不将就**——设计裂缝必须补，旧约定该改就改。
- 容错上**不周全**——不枚举所有异常分支，内部操作不做过度防御性编程。
- 最好的设计简单粗暴、概念越少越好，而不是复杂的过度设计。

**历史不是目标合同**：旧实现、旧文档、现有特例只代表当前状态。旧约定若破坏整体抽象，就统一模型并同步改必要调用方与文档；只有已确认的外部不可控兼容边界才保留特例。

**事实分层**：代码、配置与运行结果用于确认当前实现；用户已批准的需求、设计与业务文档用于确认目标合同。两者不一致时必须明确指出差距，不能用当前源码否定尚未实现的目标。

## 3 · 硬约束

1. 决策必须区分已确认事实、合理假设与待验证项，不得把猜测写成事实。
2. 修复根因并完整更新必要调用方；不附带与目标无关的重构、功能或组件。
3. 不因旧实现存在就降低已确认目标合同。
4. **新增代码禁止依赖注入**；唯一例外是 FastAPI 路由层的 `Depends`（框架契约）。
5. 未经允许不新增依赖、不新增功能 / 组件。
6. 实现新方案后，删干净被替代的旧方案与备用分支残留。
7. TS 禁止用 `any` 逃逸类型系统；`unknown` 仅限各端 spec 已批准的解析边界，必须在边界内完成收窄，禁止用断言代替校验。
8. 面向用户的文字必须走 i18n。
9. HTTP 接口只用 **GET 和 POST**。

## 4 · 仓库地图

monorepo，前端子项目独立用 **pnpm** 管理（无根 workspace），后端用 **uv**。

| 目录              | 职责                                                    | 技术栈                  | 规范                                  |
| ----------------- | ------------------------------------------------------- | ----------------------- | ------------------------------------- |
| `backend/`        | 后端 API                                                | Python + FastAPI        | spec-python / spec-mysql / spec-redis |
| `extension/`      | Maps Extractor 插件端(TG 业务已移除)                    | Chrome MV3 + Vue 3      | spec-extension                        |
| `extension-bing/` | Bing Maps Scraper 插件端(016 域,底座与 extension/ 同构) | Chrome MV3 + Vue 3      | spec-extension                        |
| `website/`        | 主站                                                    | Astro                   | spec-website                          |
| `admin/`          | 管理后台                                                | 独立 Vue + Naive UI SPA | —                                     |
| `docs/`           | 需求 / 技术 / 计划文档                                  | —                       | spec-docs                             |

**跨端事实**：客户端接口前缀 `/api/client/*`，后台前缀 `/api/admin/*`；后端业务时区统一为 `America/New_York`（见 `backend/src/app/utils/time.py`），不是 UTC。

`docs/` 结构：`ROADMAP.md` 项目进度大盘（功能全景 / 状态 / 阶段）、`feat/` 按域组织的产品与技术文档（`000.架构` 是地基域，`001`~`011` 是 Telegram 产品线业务域，`013`~`015` 是 Maps Extractor 新线域，`archive/` 是已废弃快照）、`references/specs/` 项目规范、`references/curated/` 调研数据、`research/` 可复用调研结论、`seo-skill/` SEO 内容工作区、`scratch/` 临时文件（不入 git，含 Maps 竞品逆向材料）。模块级工作先读 `ROADMAP.md` 定位所属域；小修改直接读取相关入口、调用方与按需 spec。写 / 改 docs 前读 `spec-docs.md`。

## 5 · 交付标准

- 负责实施与验证的执行者必须完成交付门禁：改动会进入运行产物的代码、依赖、构建配置或运行配置后，对受影响端**构建 + 启动**，无异常才算结束；仅改测试时运行相关测试；仅改文档时不构建、不启动，只执行文档格式、链接和内容一致性检查。可采纳当前改动对应 worker 的完整验证证据，不要求调度者重复执行；角色拆分不免除门禁。各端命令见对应 spec。
- 改动前先 grep 引用点，确认调用方是否要一起改。
- 改代码后检查 `docs/feat/` 下对应文档是否需要同步。
- 后端：改完 models 必须跑结构同步脚本 `sync_database_schema`（命令见 `spec-mysql.md`）；提交前过 black + ruff + mypy。
- 只运行能证明改动的最少测试；已有且与当前改动完全对应的证据可复用。失败、通过、因环境 skip 和未执行必须明确区分，必需验收不足时不能仅凭退出码 0 结束。
- 无相互依赖的任务尽量并行处理。

## 6 · 按需 spec（纯文字触发，不用 `@` 自动加载）

相关任务开始前主动读对应文件，均位于 `docs/references/specs/`：

`admin/` 暂无独立 spec；其脚本与 E2E 入口以 `admin/package.json`、`admin/playwright.config.ts` 和现有 README 为准。

| 何时读                                   | 文件                                          |
| ---------------------------------------- | --------------------------------------------- |
| 写 / 改任何代码                          | `spec-code.md`（注释与错误消息）              |
| 写 / 改 docs（feat / tech / plans）      | `spec-docs.md`                                |
| 加 / 改数据库索引                        | `spec-index.md`                               |
| 写 / 改后端 Python                       | `spec-python.md`                              |
| 写 / 改 Model、查询、事务、迁移          | `spec-mysql.md`                               |
| 写 / 改 Redis（缓存 / 锁 / 限流 / 队列） | `spec-redis.md`                               |
| 写 / 改 website（Astro）                 | `spec-website.md`                             |
| 写 / 改 extension（MV3）                 | `spec-extension.md`                           |
| SEO 内容创建 / 优化 / 本地化             | `spec-google-seo.md`                          |
| 写测试                                   | `spec-test-server.md` / `spec-test-client.md` |
| UI / 视觉 / 配色 / 玻璃效果              | `design.md`（亮色）/ `design.dark.md`（暗色） |
