# 008 · 管理后台

> 产品需求文档。只描述"后台有哪些管理模块 + 边界 + 用户操作 / UI"。技术实现(SPA 架构、各模块接口规格、节点本地 vs 中心 admin 入口、admin JWT-only 认证)见同目录 `tech-*.md`。
> 本域是面向运营管理员的独立后台,只承载管理能力。节点执行底座见 `@../001.节点系统/feat.md`,订单数据本身见 `@../004.订单系统/feat.md`,下载记录与上游速率口径见 `@../002.下载功能/feat.md`,积分见 `@../003.积分系统/feat.md`,客户端用户见 `@../007.用户系统/feat.md`。

## 功能目标

为运营管理员提供一个**独立部署、与 public website 物理隔离**的管理后台,集中承载日常运维与排障所需的只读查看与受控写入能力,替代此前散落在 CLI 脚本和文件系统的人工操作。

管理后台是一个独立的单页应用,不是 website 的一部分。它部署在独立域名,有自己的登录入口、自己的管理员账号体系、自己的会话与令牌。管理员通过浏览器进入后台,完成账号登录后,在侧边栏切换各管理模块。

## 功能范围

### 包含

- 管理员登录、登出、Access Token 自动续签、中英文切换。
- 运营 Dashboard:用户与打点统计(60 天按天聚合),只读。
- Telegram 客户端管理:列表查看、批量验证可用性(实时进度推送)、交互式新增登录(手机号 → 验证码 → 可选两步验证)、自动模式批量登录(粘贴「手机号 + 接码链接」列表,串行自动完成发码 / 取码 / 两步验证)、删除客户端(含活跃请求拦截与二次确认)。
- 渠道设置:当前节点 / 目标节点的 X、Instagram Cookie 本地 JSON 池管理(查看、新增、更新、删除),明文只写不读。
- 订单管理:只读查询订单列表与详情,支持多维度筛选,不做任何写操作。
- 系统设置:提供低频运维动作,支持手动刷新全部配置读取缓存、生成 / 轮换当前管理员的外部 API Key、编辑 Google 数据采集配置、Telegram DOM 稀疏覆盖对象和新版扩展使用的 Telegram Config 稀疏配置对象。
- 外部 API:第三方系统可用管理员生成的 API Key 访问独立的外部 API 分组,当前只提供系统统计大盘。
- 通用用户信息弹窗:后台内所有可点击的用户 ID 都可打开同一个用户详情弹窗,展示用户基础信息、Credits、订阅状态、最近下载记录和订单列表。
- 管理后台所有时间展示统一为 `YYYY-MM-DD HH:mm:ss`,固定按 UTC+8 显示。
- 下载详情列表:只读查看下载开始 / 成功 / 失败三类记录,辅助排障。
- 解析失败排查:只读查看解析失败打点,并对单条失败链接触发一次重新解析。
- 服务节点管理:节点的增删改、启用 / 停用、手动健康检查(详见 `@../001.节点系统`)。
- 节点本机监控:查看各节点本机网络进出速率。
- 数据分析:下载资源分布(按大小分桶,展示次数占比)、下载用户排名、下载统计汇总、用户注册地区分布(展示人数占比)、每日充值(+8)、商品统计(+8),六个只读子标签页,统一时间范围筛选。
- 管理员账号通过 CLI 创建,不在后台 UI 增删改。

### 不包含

- 客户端用户的下载、解析、播放等面向终端用户的业务流程(属 `@../002.下载功能`)。
- 订单状态机、支付、履约一致性规则本身(属 `@../004.订单系统`);本域只读展示订单,不修改订单。
- 节点调度规则、健康检查调度、节点发布配置(属 `@../001.节点系统`);本域只提供节点管理 UI 与节点本地运维入口。
- 下载业务的接口规格、token 设计(属 `@../002.下载功能`)。
- 客户端用户认证、用户注册登录(属 `@../007.用户系统`)。
- 节点本机 admin 本地认证(属 `@../001.节点系统` 的节点本地管理边界)。
- 管理员角色的细粒度权限控制(RBAC):当前所有管理员权限相同。
- 管理员的增删改查 UI:通过 CLI 管理。
- 多 API Key 管理、API Key 权限分组、API Key 调用审计、API Key 过期策略。
- Dashboard 的图表可视化:当前仅数据表格。
- 订单的退款、取消、改状态、手动补履约、导出、批量操作。
- 客户端用户的编辑、封禁、删除、改邮箱、代充、人工改订阅;通用用户信息弹窗本期只读。
- 通用配置表编辑器、Google 数据采集、Telegram DOM 和 Telegram Config 三个明确入口以外的任意配置值读写、配置版本管理、配置审计、跨节点缓存广播。
- Cookie 有效性定时巡检、平台账号登录自动化、用户个人 Cookie 上传。
- 监控的历史曲线、趋势图、告警、峰值统计。
- 数据分析的图表可视化:当前仅表格 + 统计卡片,不引入图表库。
- 数据分析的 extension 下载口径:基于 website 付费下载记录统计,不含 extension 下载。
- 通用用户信息弹窗的下载筛选、订单筛选、导出、extension 下载历史、积分流水和风控操作;本期只实现「最近下载」和「订单列表」tab。

### 域边界事实(四套认证边界)

系统同时存在四套互不混用的认证边界,管理后台负责其中两套:

1. **管理后台 admin 认证(本域)**:独立的管理员账号表 + 账号密码 + 图片验证码登录,签发独立的 admin JWT(Access + Refresh)。Access Token 默认有效期 30 分钟,Refresh Token 默认 7 天。业务服务器负责登录、签发与续签;节点本地管理接口只验签 Access Token,不查业务数据库、不查 Redis。
2. **客户端用户认证(`@../007.用户系统`)**:面向终端用户的登录与会话,与管理后台账号表、admin JWT 完全独立。
3. **节点 admin 本地认证(`@../001.节点系统`)**:节点本地管理接口的 JWT-only 验签,复用同一支 admin Access Token 做纯验签,但不在节点侧回查数据库或 Redis,也不提供登录 / 续签。
4. **外部 API Key 认证(本域)**:管理员在系统设置页生成 API Key,业务服务器只保存 hash、前缀和生成时间;第三方系统用该 key 访问独立外部 API 分组。API Key 不等价于 admin JWT,不能登录后台,不能调用管理后台接口。

四条边界的服务对象、令牌来源、是否回查存储各不相同,实现上禁止互相混用鉴权依赖。

## 业务流程

### 管理员登录(主流程)

1. 管理员打开后台登录页。
2. 前端请求图片验证码。
3. 管理员输入用户名、密码、验证码。
4. 后端校验验证码 → 查管理员账号表 → 校验密码哈希 → 检查账号启用状态 → 签发 Access / Refresh Token。
5. 前端保存令牌,跳转后台首页。
6. 后续请求携带 Access Token;过期时前端拦截器用 Refresh Token 自动续签并重试一次,续签失败跳回登录页。

### 模块内操作(通用模式)

- 只读模块(Dashboard、订单、下载详情、解析失败排查):进入页面 → 按筛选条件请求列表 → 查看;少数解析失败排查支持对单条触发一次重新解析。
- 受控写入模块(TG 客户端、媒体 Cookie、服务节点、系统设置):列表查看 + 表单 / 对话框写入,关键操作按业务风险使用显式保存、二次确认或活跃状态拦截。
- 节点维度操作:管理员从业务服务器读节点列表后,按目标节点公网地址**直连目标节点本地接口**执行 TG 客户端 / 媒体 Cookie / 监控相关操作,不经业务服务器代理。

### 异常与分支

- 令牌过期且续签失败:清空令牌,跳登录页。
- 目标节点直连返回鉴权失败:前端回业务服务器续签后重试一次,仍失败则展示节点鉴权失败。
- Telegram 登录被限流 / 手机号被封 / 验证码过期:对应明确提示,可重试或更换输入。
- 删除有活跃请求的客户端:被服务端拒绝,提示活跃请求数。
- Cookie 池文件缺失或全部失效:X 继续游客 Playwright Cookie 兜底;Instagram 返回匿名解析错误,管理员可更新 Cookie 后由用户重试。
- 监控采样无效或非 Linux 环境:展示无数据,不影响其他模块。
- API Key 泄漏:管理员在系统设置页重新生成即可让旧 key 失效。
- 外部统计大盘读取部分节点失败:接口仍返回业务统计与其他节点数据,失败节点展示空客户端列表 / 空速率和错误摘要。
- Telegram DOM 文本不是合法 JSON 或顶层不是对象:页面不提交,保留当前输入并提示管理员修正。
- Telegram DOM 读取或保存失败:只影响本标签页,不自动重试;管理员可再次读取或保存。
- Telegram Config 文本不是合法 JSON 或顶层不是对象:页面不提交,保留当前输入并提示管理员修正。
- Telegram Config 读取或保存失败:只影响本标签页,不自动重试;管理员可再次读取或保存。

## 非功能性需求

- 后台是低频内部工具,监控 / 订单等只读页面不承诺大数据量或宽时间窗下的查询性能,只面向人工排查。
- 后台独立域名部署,与 public website 物理隔离;Nginx 对实时推送类接口关闭缓冲并放宽超时。
- 写入操作避免在管理路径直接持有业务长事务,参数校验在 API 层,事务在 service 层。
- 批量删除 / 修改走 SQL 层,不先全量加载再逐行改。
- 后台文案必须支持 i18n(中英文)。
- Cookie 明文不进入任何 API 响应,不写后端日志。
- API Key 原文只在生成成功响应里展示一次;后端数据库、日志和普通查询响应都不保存 / 不回显原文。
- Telegram DOM 是面向多个扩展版本的宽松稀疏对象:允许缺少项和多余项,不检查字段完整性、字段类型或 selector 语法。
- Telegram Config 是面向新版扩展的独立宽松稀疏对象:允许缺少分组、缺少字段和未知项,不继承或改写 Telegram DOM 内容。

## 验收标准

产品视角的关键验收(技术层验收见各 `tech-*.md`):

- 管理员可通过 CLI 创建账号,并用账号密码 + 图片验证码登录后台。
- 登录后进入 Dashboard,能看到正确的用户统计与 60 天按天打点数据;插件商店评价点击按日展示点击次数与独立设备数。
- TG 客户端列表正确展示;批量验证逐条返回结果,状态正确;可完成完整的交互式新增登录(手机号 → 验证码 → 可选两步验证);自动模式可粘贴多号 + 接码链接列表,串行完成登录并展示每号结果;可删除无活跃请求的客户端,删除前有二次确认。
- X、Instagram Cookie 管理可分别查看、新增、更新、删除当前 / 目标节点的本地池记录,任何列表和响应都不回显 Cookie 明文。
- 系统设置页可手动刷新全部配置读取缓存;成功后后续配置读取使用数据库最新值,失败时展示错误并允许管理员重试。
- 系统设置页可生成 / 重新生成当前管理员的 API Key;生成成功后只展示一次完整 key,刷新页面后只能看到 key 前缀与生成时间。
- 系统设置页的 Telegram DOM 标签默认显示 `{}`;管理员可保存任意稀疏顶层对象,缺少项和未知项不会被页面拒绝或删除。
- Telegram DOM 标签不展示插件内置默认值,不提供“恢复默认”、格式化、自动保存、版本选择、历史或回滚;保存成功后提示刷新 Telegram 页面生效。
- 系统设置页的 Telegram Config 标签默认显示 `{}`;管理员可独立保存任意稀疏顶层对象,且不会改写 Telegram DOM 标签内容。
- Telegram Config 标签不展示扩展包内默认值,不提供“恢复默认”、格式化、自动保存、版本选择、历史或回滚;保存成功后提示刷新 Telegram 页面生效。
- 使用有效 API Key 可访问外部系统统计大盘;无 key、错 key、已被重新生成覆盖的旧 key 不能访问。
- 系统统计大盘返回今日注册人数、今日付费订单总额、今日履约失败数量与金额、当前节点列表、每个节点的 TG 客户端列表和下载 / 上传速率。
- 订单管理可按多维度筛选并查看订单列表与详情,且不存在任何修改订单状态的按钮或接口。
- 点击后台中展示的用户 ID 可打开通用用户信息弹窗;弹窗只读展示用户基础信息、当前 Credits、当前订阅状态、`user_download_records` 最近下载列表和该用户全部订单列表,两个列表均支持分页。
- 下载详情列表只展示下载开始 / 成功 / 失败三类记录,坏数据不导致页面崩溃。
- 解析失败排查可查看失败记录并对单条触发一次重新解析。
- 服务节点管理可新增、编辑、启用 / 停用、手动健康检查节点(节点 CRUD 细节见 `@../001.节点系统`)。
- 节点本机监控可在服务节点列表与 TG 客户端管理页展示各节点网络进出速率或无数据占位。
- Access Token 过期前自动续签,过期且续签失败后跳转登录页。
- 中英文切换正常,移动端基本可用。

## 用户操作逻辑与 UI 元素

> 后台 UI 文案需 i18n。所有模块共用一个带可折叠侧边栏与顶栏(语言切换、Admin 标识、登出)的布局。

### 登录页

- 居中卡片,最大宽度 400px。
- 表单:用户名输入、密码输入、验证码输入 + 验证码图片(120px × 40px,点击刷新)。
- 登录按钮(带 loading),成功后跳首页。

### Dashboard 首页

- 顶部统计卡片:总用户数、今日新增(展示昨日同期人数与变化百分比)、24H 活跃人数、7D 活跃人数。
- 60 天按天数据表格:日期、注册数、各打点类型的事件数 / 设备数,窄屏横向滚动。admin 表格动态打点列第一列展示 `web_first_opened`;隐藏 `web_parse_input_click`、`web_download_click`、`web_page_open`,并把 `content_open`、`download_click`、`popup_open` 放到末尾展示;后端与数据库仍保留完整打点。

### TG 客户端管理页

- 顶部工具栏:新增客户端按钮、全选、批量验证按钮、刷新按钮。
- 客户端表格:手机号、session 文件状态、活跃请求数、是否已加载、健康状态、操作(删除)。
- 批量验证:实时进度条 + 每行状态实时刷新(成功 / 失败 / 锁定)。
- 新增客户端对话框(不可通过 ESC 或遮罩关闭),含两个模式切换:
  - 交互式模式:三步表单(手机号 → 验证码,含 120 秒倒计时重发 → 可选两步验证),底部取消按钮,成功自动关闭并刷新列表。
  - 自动模式:多行文本输入(每行「手机号 + 该号的接码链接」),确认后串行处理每个号,实时展示进度与每号结果(已登录 / 未登录 / 失败原因)。同一时刻只处理一个号;某号失败可中止后续。接码链接由后台代为请求并解析,后台按链接自动识别接码平台,管理员无需手动选择。
- 删除:行内删除按钮 + 二次确认弹窗,有活跃请求时拒绝并提示数量。
- 节点维度:页面按节点分组,每组 header 展示节点信息与本机网络速率。

### 渠道设置 / 媒体 Cookie 页

- 顶部平台 Tab:X、Instagram;默认 X,首次切换时加载对应平台数据。
- 各平台按节点展示池状态:文件是否存在、更新时间;健康但停用的节点仍允许配置。
- Cookie 记录表格:名称、启用状态、格式、平台关键字段检测结果、最近成功 / 失败、更新时间、操作;窄屏表格横向滚动。
- 新增 / 编辑表单:标题和占位文案标明平台,包含名称、启用开关、Cookie 文本框;编辑时文本留空表示不替换明文。
- 任何位置不展示 Cookie 明文。
- 节点维度:默认展示全部节点区块;从服务节点入口携带目标节点时只展示该节点。

### 订单管理页

- 筛选区:订单号、用户 ID、用户当前邮箱、支付渠道单号、订单状态、履约状态、商品、支付方式、创建时间范围。
- 订单表格:订单号、用户 ID、用户当前邮箱、商品、订单状态、履约状态、支付方式、订单金额、实付金额、渠道单号、创建时间、支付时间,以及详情入口。
- 订单表格和详情抽屉中的用户展示是可点击入口,点击打开通用用户信息弹窗。
- 详情抽屉:订单基础信息、商品快照、金额快照、支付信息、时间信息、原始支付数据 JSON、扩展元数据 JSON,支持复制订单号与渠道单号。
- 用户当前邮箱明确标注"当前邮箱,非下单时快照"。
- 不存在任何修改订单状态的按钮或接口。

### 系统设置页

- 页面标题:系统设置。
- 顶部标签栏包含「配置表缓存」「API Key」「Google 数据采集」「Telegram DOM」「Telegram Config」五个标签,点击切换;各标签的加载或保存失败互不影响。
- 配置缓存区块:普通内容区,展示一个"刷新配置缓存"按钮和最近一次操作结果。
- 刷新配置缓存按钮:主按钮,可点击;点击后进入 loading,调用后台刷新全部配置读取缓存;请求完成后恢复可点。
- 成功结果:展示成功提示和本次刷新时间;管理员可继续重复点击。
- 失败结果:展示失败提示;不做自动重试,管理员可再次点击。
- API Key 区块:展示当前管理员是否已有 API Key、key 前缀、生成时间和"生成 API Key" / "重新生成 API Key"按钮。
- 生成 API Key 按钮:主按钮,首次生成时可直接点击;生成成功后弹窗展示完整 key,提示只展示一次。
- 重新生成 API Key 按钮:危险按钮,点击前二次确认;确认后生成新 key 并立即让旧 key 失效。
- 复制 API Key 按钮:只在生成成功弹窗内显示,点击复制完整 key。
- Telegram DOM 编辑区:一个全宽等宽字体多行文本框,最小高度 360px;首次没有服务端内容时显示 `{}`,不把插件包内默认 DOM 填入文本框。
- Telegram DOM 保存按钮:主按钮,管理员点击后才提交;保存中进入 loading 并禁止重复提交。成功后保留原输入并提示刷新 Telegram 页面生效。
- Telegram DOM 错误态:JSON 解析失败或顶层不是对象时在编辑区下方提示且不提交;读取失败时显示重试入口;保存失败时保留未保存文本并允许再次点击。
- Telegram DOM 不提供字段表单、字段补全、自动格式化、自动保存、版本选择、“恢复默认”、历史或回滚。管理员要取消全部远端覆盖时手工保存 `{}`。
- Telegram Config 编辑区:一个全宽等宽字体多行文本框,最小高度 360px;首次没有服务端内容时显示 `{}`,不展示扩展包内默认值。
- Telegram Config 保存按钮:主按钮,管理员点击后才提交;保存中进入 loading 并禁止重复提交。成功后保留原输入并提示刷新 Telegram 页面生效。
- Telegram Config 错误态:JSON 解析失败或顶层不是对象时在编辑区下方提示且不提交;读取失败时显示重试入口;保存失败时保留未保存文本并允许再次点击。
- Telegram Config 不提供字段表单、字段补全、自动格式化、自动保存、版本选择、“恢复默认”、历史或回滚。管理员要取消全部远端覆盖时手工保存 `{}`。
- 除 Google 数据采集、Telegram DOM 和 Telegram Config 三个明确入口外,本页不提供通用配置值编辑器;也不提供 API Key 多条管理、权限分组或审计列表。

### 外部系统统计大盘

- 第三方系统通过 API Key 请求独立外部 API 分组中的系统统计大盘。
- 返回今日注册人数。
- 返回今日付费订单总额;不按商品类别过滤,履约成功与履约失败都计入,按币种分别返回,不把不同币种金额混加。
- 返回今日履约失败订单数量和金额;该字段用于外部系统重点展示异常收入。
- 返回当前节点列表:每个节点含节点名字、TG 客户端列表、节点下载 / 上传速率。
- 节点不可访问或采样无数据时,该节点保留在列表中,对应客户端列表为空或速率为空,并带错误摘要。

### 下载详情列表页

- 刷新按钮 + 远程分页表格。
- 列:ID、状态(开始 / 成功 / 失败)、媒体平台、节点 ID、地址、文件大小、文件名、错误内容(仅失败行)、时间。
- 字段缺失统一显示占位符;坏数据不崩溃。

### 解析失败排查页

- 远程分页表格:解析失败打点记录。
- 单条操作:对失败链接触发一次重新解析,展示结果。

### 服务节点管理页

节点 CRUD、启用 / 停用、手动健康检查、节点本地运行态诊断。UI 细节与可用条件属节点系统域,见 `@../001.节点系统/feat.md`。

### 节点本机监控展示

- 服务节点列表新增"当前速率"列(入站 / 出站两行)。
- TG 客户端管理页节点 header 展示入站 / 出站速率。
- 无有效数据、节点停用 / 不健康 / 不可直连时显示占位符。

### 数据分析页

- 顶部标签栏(NTabs)六个只读子页:下载资源分析 / 下载排名 / 下载统计 / 用户地理分析 / 每日充值(+8) / 商品统计(+8),点击切换。
- 每个子页顶部统一筛选区:时间范围选择器(默认最近 24 小时,提供「最近 24 小时」「最近 7 天」快捷,可手动选区间)+ 查询 / 刷新按钮。
- 下载资源分析:表格,列「资源大小(档位) / 次数(消耗积分) / 次数(百分比%) / 次数(总和) / 次数(百分比%)」,固定 12 档(11 个大小档 + 未知大小),空档显示 0;两个百分比分别按当前结果中消耗积分次数总和、全部次数总和计算。
- 下载排名:表格,列「排名 / 用户 ID / 次数(消耗积分) / 次数(总和)」,按总和次数降序取前 20,排名为行号。
- 下载排名里的用户 ID 可点击打开通用用户信息弹窗。
- 下载统计:6 个统计卡片(人数 / 次数 / 资源大小总和,各分「按消耗积分」「总和」),资源大小显示为 MB/GB。
- 用户地理分析:表格,列「地区(国家名,未知显示占位) / 人数」,人数单元格显示为「人数 (百分比%)」,百分比按当前结果总人数计算;地区为注册国家,只有国家粒度。
- 每日充值(+8):表格,列「日期 / 笔数(成功) / 金额(成功) / 笔数(全部) / 金额(全部)」;日期按 UTC+8 自然日,成功为订单状态=已支付,全部为所有订单;金额按币种拆分展示,不混加。
- 商品统计(+8):表格,列「日期 / 商品 ID / 笔数(成功) / 金额(成功) / 笔数(全部) / 金额(全部)」;日期按 UTC+8 自然日,按商品 ID 分组;成功与全部口径同每日充值。
- 各子页无数据显示空态;区间或参数异常展示错误提示,不崩溃。

### 通用用户信息弹窗

- 入口:后台内所有表格或详情里展示的非空用户 ID,包括订单管理用户列 / 详情、数据分析下载排名、TG 客户端当前使用明细。
- 弹窗形态:居中 modal,最大宽度约 960px,移动端宽度随视口收缩;关闭按钮位于右上角。
- 顶部信息:用户 ID、邮箱、注册来源、注册方式、注册 IP(归属地)、最后登录 IP(归属地)、最后一次操作 IP(归属地)、注册时间、最后登录时间、登录次数、账号状态。
- 权益信息:当前 Credits 余额、是否有有效订阅、订阅过期时间。订阅已过期时显示未订阅,同时保留过期时间用于排查;从未订阅时过期时间显示占位符。
- 下方 tabs:
  - 「最近下载」:来源为 `user_download_records`,展示平台、链接、source ID、文件名、文件大小、扣除 Credits、下载时间,支持远程分页,不提供筛选。
  - 「订单列表」:展示该用户全部订单,成功/失败/待支付/取消/过期都显示,支持远程分页,不提供筛选。
- 异常:用户不存在或接口失败时展示错误提示并允许关闭后重试;字段缺失统一显示占位符。

## 关联文档

- SPA 架构、前端栈、路由、请求封装、令牌续签、部署:`@tech-后台架构与认证.md`
- 管理模块接口索引与通用约定:`@tech-管理模块接口.md`
- 菜单级技术文档:`@tech-Dashboard.md` `@tech-数据分析.md` `@tech-服务节点.md` `@tech-TG客户端.md` `@tech-日志排查.md` `@tech-下载详情.md` `@tech-订单管理.md` `@tech-渠道设置.md` `@tech-系统设置.md`
- 通用后台能力:`@tech-用户信息弹窗.md`
- 外部 API 分组与系统统计大盘接口:`@tech-外部API.md`
- 节点本地入口与中心 admin 入口的双路由边界、节点直连模式:`@tech-节点本地与中心入口.md`
- 变更记录:`@changelog.md`
- 节点底座、节点 CRUD、节点本地管理架构:`@../001.节点系统/feat.md`
- 订单数据与状态机:`@../004.订单系统/feat.md`
- 下载记录、上游速率口径:`@../002.下载功能/feat.md`
- 积分:`@../003.积分系统/feat.md`
- 客户端用户:`@../007.用户系统/feat.md`
