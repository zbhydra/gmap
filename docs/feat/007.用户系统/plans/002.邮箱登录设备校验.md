# 002 · Website 设备可信校验

## 目标

为 website 重要客户端接口增加设备校验前置条件。用户正常打开页面后无需感知;若校验缺失或过期,接口直接提示刷新页面后重试。

## 依赖

- 产品口径:`@../feat.md`
- 技术方案:`@../tech-邮箱登录设备校验.md`
- website 设备 ID:`@website-shared/src/homepage-runtime/device.ts`
- website Layout footer:`@website/src/layouts/Layout.astro`
- 后端认证 API:`@backend/src/app/api/client/auth_client.py`
- 后端请求上下文:`@backend/src/app/api/user_dependencies.py`
- Redis 规范:`@docs/references/specs/spec-redis.md`

## 改动范围

### website

1. `Layout.astro` footer 增加 `<span data-footer-credits-icon></span>`。
2. `website-shared/src/homepage-runtime/device.ts` 增加:
   - `CLIENT_UUID_COOKIE_NAME`
   - 写 Cookie 方法。
   - 插入 `/assets/icons/credits.svg` 图片的方法。
   - 页面初始化入口。
3. `website/astro.config.mjs` 增加 Vite dev server 精确 proxy:`/assets/icons/credits.svg` → `PUBLIC_API_BASE_URL || http://localhost:9600`。
4. `website/src/scripts/homepage/device.ts` 继续 re-export 新能力。
5. `Layout.astro` 的全局脚本调用初始化入口。

### backend

1. 新增 `backend/src/app/services/device_service.py`:
   - `set(device_id, ip)`
   - `verify(device_id, ip)`
2. 新增 SVG 资源路由,路径固定为 `/assets/icons/credits.svg`。
3. `main.py` business role 挂载该资源路由。
4. `CommonCode` 新增 `AUTH_PAGE_REFRESH_REQUIRED`。
5. 14 个后端 i18n locale 增加刷新重试文案。
6. `auth_client.py` 在 `send-email-code` 和 `email-verify-login` 前通过 `device_service.verify_request_device(...)` 校验。
7. `media_pre_v2_client.py` 在 `parse-pre-v2` 和 `download-pre-v2` 前通过 `device_service.verify_request_device(...)` 校验。

### deploy

1. `website/deploy/tg-web.conf` 增加精确 `location = /assets/icons/credits.svg`。
2. `website/deploy/tg-web-test.conf` 增加同名规则,反代到测试后端。

## 验收标准

1. 页面加载后可以在浏览器 Cookie 中看到 `client_uuid`。
2. Network 面板可以看到 `/assets/icons/credits.svg` 返回 SVG,不是 JSON。
3. SVG 响应头包含 `Cache-Control: no-store`。
4. 本地 `pnpm dev` 下该图片请求通过 Vite proxy 命中本地后端;不依赖 `website/public` 静态 SVG。
5. 已建立 Redis 记录后,`send-email-code` 正常发送验证码。
6. 删除 Redis 记录或修改 IP 后,`send-email-code` 返回 `AUTH_PAGE_REFRESH_REQUIRED`。
7. 删除 Redis 记录后,`email-verify-login` 不消耗验证码尝试次数。
8. SVG 接口缺 Cookie 或 Redis 写入异常时仍返回 SVG。
9. SVG 接口内部未捕获异常被路由捕获为 404,不返回 JSON 错误。
10. 删除 Redis 记录后,`parse-pre-v2` 在 IP 限流前返回 `AUTH_PAGE_REFRESH_REQUIRED`。
11. 删除 Redis 记录后,`download-pre-v2` 在用户短锁前返回 `AUTH_PAGE_REFRESH_REQUIRED`。
12. Google 登录、订单、支付、mark-log、`parse-v2`、`download-v2` 不受影响。

## 验证命令

后端:

```bash
cd backend
uv run black src tests
uv run ruff check src tests
uv run mypy src
uv run pytest tests/test_server/api/test_auth_api.py tests/test_server/api/test_media_pre_v2_client.py
```

website:

```bash
cd website
pnpm build
node --test tests/module-scripts.test.js
PUBLIC_API_BASE_URL=http://localhost:9600 pnpm dev --host 127.0.0.1 --port 4321
```

部署检查:

```bash
nginx -t
curl -i https://telegramdownloadmedia.com/assets/icons/credits.svg
```

## 风险与回滚

- 风险:用户换 IP 后邮箱登录被拦截。处理:提示刷新页面后重试,刷新会重新建立绑定。
- 风险:website nginx 规则顺序错误导致静态 404。处理:精确 location 必须放在通用 `.svg` 静态 location 前。
- 风险:Redis 故障导致受保护的重要 API 不可用。处理:按 fail-closed;可临时回滚对应 API 校验调用。
- 回滚:把 `config_public.device_trust.verify_device_id` 改为 `false`;保留 SVG 和 Cookie 写入不影响主流程。
