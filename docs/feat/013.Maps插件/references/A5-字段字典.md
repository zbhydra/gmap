# A5 · 字段字典 36 列(竞品调研)

> Roadmap:A5 · 调研:🔍 完成(实测 20/20 命中)· 深读:scratch 逆向 04(36 列逐个下标)/ 03 §3.1

## 功能定义

搜索抓取的标准导出 schema:36 个导出列,固定顺序(= 默认勾选顺序 = CSV 列序),其中 12 列为 Pro 专属。

## 用户操作(竞品)

- 设置页/面板勾选导出字段(默认全选);免费用户勾 Pro 列在生成导出时被静默剔除。
- 去重:解析时按 Cid+Name(滚动重复批),导出时按 Place Id(先到先得)。

## 竞品实现逻辑(36 列清单)

| 列 | 来源 | Pro |
| --- | --- | --- |
| Name / Description / Fulladdress / Street / Municipality / Categories / Time Zone / Phone / Phones / Amenities / Claimed / Review Count / Average Rating / Review URL / Latitude / Longitude / Website / Domain / Opening Hours / Featured Image / Cid / Fid / Place Id | 搜索 RPC 固定下标(实测 100% 有效,下标表见 scratch 04) | |
| Owner / Owner Id / Owner Link | RPC 下标 / 衍生 contrib 链接 | ✓ |
| Email / Social Medias | 服务端代抓(A4) | ✓ |
| Plus Code | RPC + 服务端按经纬度补 global_code | ✓ |
| Price / Note / Hotel Class / About | RPC 下标 | ✓ |
| Google Knowledge URL | 衍生 `search?kgmid=` | ✓ |
| Google Maps URL | 衍生 `maps?cid=`(稳定短链) | |
| Kgmid | RPC 下标 | ✓ |

衍生规则:Cid = fid 第二段 hex 转十进制;Opening Hours 输出 `周几(日期): [HH:mm-HH:mm,…]`,歇业标 Closed;地址超 200 字符置空防脏数据。文件命名 `G-Maps-Extractor-{条数}-{关键词}-{YYYY-MM-DD}`。

**评论 11 列**(A2):Author / Review Text / Rating / Date / Photos / Likes / Owner Answer / Owner Answer Date / Author Profile / Author Image / Review URL。

## 我方落地要点

- 下标路径即 parseSchema,走远程配置(同 A1);`len-8` 列表定位与 V1/V2 双格式分支是版本兼容点。
- Pro 字段门控与 003/006 的套餐权益对齐;我方免费/付费分界列可自行决策(不必照抄 12 列)。
- 数据缺口(社媒 6 列)与反向超出(全量评论对象、PriceRange、StreetView)参照 `research/google-maps-scraping-方案调研.md` §5。
