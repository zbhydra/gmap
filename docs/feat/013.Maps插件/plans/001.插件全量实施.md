# 001 · 插件全量实施(执行计划)

> 执行单元拆分、边界、验收与状态表。依据:`@../feat.md`(需求合同)、`@../references/`(竞品证据)、`@../../../scratch/G-MAPS-EXTRACTOR-v2.5.1/research/11-复刻问题清单与决策.md`(已拍板架构决策,域内副本 `@../references/竞品逆向/11-复刻问题清单与决策.md`)。
> 状态机:`pending → ready → running → review → fix → done`;`blocked` = 需用户决策。

## 0 · e2e 基建设计(U2 交付,全域复用)

目标:**默认 e2e 完全自动化,零人工干预,可重复**。

- **project**:`extension-mv3`(Playwright,`--load-extension=<dist>` + `--disable-extensions-except`,fresh-built;沿用 extension/ 现有 playwright 底座与浏览器身份机制)。
- **fixture 路由模式**:双 pattern 拦截——`page.route('https://www.google.com/maps/**')` 以本地 fixture fulfill(按 013 实测 DOM 形态构造的列表/详情页 HTML,交互链路);`page.route('https://www.google.com/search*')` 以黄金样本 fulfill(解析链路,Maps 内部 RPC 走 `/search?tbm=map`,不匹配 `/maps/**`)。**XHR 回放机制**:fixture 页内嵌回放脚本向 `/search?tbm=` 发起 XHR,injected hook 捕获真实形态流量,采集计数/导出断言成立。插件 content script 在真实 Maps URL 上激活。
- **SW 侧零外网策略**:Playwright 的 route 拦截不到 MV3 service worker 发起的请求(上游已知限制),因此 e2e 使用独立构建变体 `build:e2e`:构建期注入 `__API_BASE_URL__=http://127.0.0.1:<port>` 指向本地 mock 服务(globalSetup 拉起,提供配置下发与打点接收端点),background 的配置拉取与埋点全部落在本地 mock——零外网、零真实遥测。
- **断言面**:面板三态出现与切换、采集计数增长、导出下载事件(download API)、批量任务落盘与恢复。
- **单测**:黄金样本 → 解析器逐字段断言(格式 A/B 双形态);远程配置覆盖/回退;状态机步进。
- **real smoke**(可选,不进默认集):真实 Maps 采集 1–2 条,单独 project 显式指定才跑——防风控,不满足「无人工干预」约束的部分不入默认。

## 1 · 执行单元

| # | 单元 | 范围 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| U1 | 工程底座 | 远程配置通道(dom/parseSchema/scrape 三组,复用 tg-download 方案)+ **backend 最小只读下发端点 `GET /api/client/maps/config`**、content/injected/background 骨架、manifest 改造(matches `*.google.com` + Edge/Firefox 目标)、黄金样本 fixture 入库 | 无(backend 队列首位) | ready |
| U2 | A1 搜索闭环 + e2e 基建 | injected hook XHR、下标解析器(格式 A/B)、滚动驱动、面板三态、CSV 导出、e2e 基建(§0)+ 黄金样本单测;埋点:search / export_results | U1 | pending |
| U3 | A2 评论 + A3 照片 | rsScript 等价物(评论 RPC + 翻页 + 11 列)、照片 RPC + 过滤;沿用 U2 e2e 模式;埋点:scrape_reviews_content | U2 | pending |
| U4 | A5 字段 + A8 导出 | 36 列全 schema(含 Pro 门控)、三格式导出、命名/去重规则 | U2 | pending |
| U5 | A6 批量面板 | dashboard 页、双任务类型(关键词类型依赖 U2 即可;评论 URL 任务类型依赖 U3)、状态落盘步进状态机、chrome.alarms 恢复 | U2;评论任务类型依赖 U3 | pending |
| U6 | A9 设置 + A12 运营 | options 页(五档间隔/格式/字段/开关/重试)、远端公告/版本通道、**打点通道与事件常量**(各业务事件由归属单元自行接入:search/export→U2,评论→U3,sync→U9) | U1 | pending |
| U7 | A11 账号配额 | backend 007/003 扩展(可选登录 + 凭证签发 + 配额扣减)+ popup 账号区 + **面板配额门控**(月配额 ≥100% 禁止开始并引导订阅、免费单批上限 10 条) | U1;面板门控部分依赖 U2;backend 队列:U1 → U7 → U8 | pending |
| U8 | A4 Email/社媒 | backend 服务端自研(官网爬 + 外链解析)+ 插件接入与计量上报(依赖 U7 的凭证与配额客户端) | U7(硬依赖:计量与 Pro 门控需要凭证体系) | pending |
| U9 | A10 集成 | Drive 直传 + HubSpot 同步;埋点:sync_to_google_drive | U2 | pending |
| U10 | A7 列表模式 | 宽松触发(`10m1!1e1`)、聚合页/深层列表双容器选择器(远程配置)、列表滚动;含登录态实测校准 | U2;需 hydra 登录环境配合一轮 | pending |
| U11 | A13 上架收尾 | Edge Add-ons / Firefox AMO 构建产物、隐私政策页、商店资产、Firefox 事件页调度分支 | 全部 | pending |

## 2 · 依赖与并行

- 串行链:U1 → U2 → {U3, U4, U5, U9, U10} → U11。
- U6 依赖 U1;backend 队列 U1 → U7 → U8 三个单元互相串行,可与插件侧单元并行(U7 的面板门控部分另依赖 U2)。
- 并行规则:同一 worker 不并发两个单元。
- **契约文件互斥**:core/types.ts 与远程配置 schema 文件是全计划共享契约——同一时间只允许一个单元修改它们;并行单元各自的功能文件独立并行,需要扩展契约时向架构师申请,由架构师调度顺序(该单元的契约扩展在单元内完成后放锁)。汇合后整体 reviewer 只审跨单元契约与组合行为。

## 3 · 通用验收与验证命令(每单元)

- `pnpm build`(含 vue-tsc)、`pnpm lint`(零警告)、`pnpm test:unit:run` 全绿。
- U2 起加:e2e 相关 spec 全绿(默认集,全自动)。
- U1 另含 install 埋点(GA4 + 后端双报,SW onInstalled);btn_click 由各业务单元经 U6 通道按需上报。
- 涉及 backend 的单元(U1 的 config 端点、U7、U8)加:`uv run pytest`(相关测试)、`uv run black --check .`、`uv run ruff check .`、`uv run mypy`(改动范围);涉及模型的跑 `sync_database_schema`。
- 每单元交付物:实现 + 直接覆盖验收行为的最少测试 + 验证结果摘要;禁止假设性路径测试。

## 3.1 · UI token 约束(hydra 2026-08-30)

全项目 UI 一致性合同:`docs/references/specs/design.md`(亮色,交互与组件规则唯一来源)+ `design.dark.md`(暗色 token)。**所有涉及界面的单元(U2 面板、U5 dashboard、U6 options、U7 popup 账号区)必须消费该 token 集合(亮暗双主题),禁止硬编码色值/字体/圆角/阴影**;产品名「GMap Extractor」。reviewer 将硬编码样式作为 finding。

## 4 · 已批准基线(不重问)

1. 不强制登录(A11);2. 远程配置三组方案(U1);3. 状态落盘步进状态机(U5);4. 服务端自研 Email/社媒(U8);5. 不上 Chrome Web Store,目标 Edge + Firefox(U11);6. 失败不静默;7. 黄金样本为解析正确性基线。

## 5 · 状态日志

- 2026-08-30 计划创建。U1 ready,其余 pending。
- 2026-08-30 计划复验:9 项修复 8 项直接通过,补修 4 项簿记(N1 路由双 pattern;N2 U7 面板门控依赖 U2;N3 §2 摘要与 backend 队列同步;N4 埋点分配落到 U2/U3/U9 范围行 + install 归 U1)。reviewer 确认后计划进入执行。
- 2026-08-30 增补全局 UI 约束(§3.1):项目 UI token 合同 design.md/design.dark.md(Material You,亮暗双主题)已由另一会话完成,涉界单元强制消费,产品名定为「GMap Extractor」。U1 重派(此前因该约束暂停)。
- 2026-08-30 计划审查修复 9 项 finding:F1 面板门控归 U7;F2 远程配置服务端宿主 = backend 只读端点(U1,backend 队列 U1→U7→U8);F3 U5 评论类型依赖 U3;F4 U8 硬依赖 U7;F5 U6 只建通道、各单元自接埋点;F6 fixture XHR 回放机制;F7 e2e 构建变体指向本地 mock(SW 流量不可 route);F8 契约文件互斥机制;F9 backend 验证命令补齐。
