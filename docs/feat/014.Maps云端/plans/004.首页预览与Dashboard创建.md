# 首页预览与 Dashboard 创建实施清单

> 2026-09-07 hydra 批准执行，实施与独立审核完成。合同见 [方案](../tech-首页预览与任务创建.md)。

## 依赖与执行

方案已批准执行。后端接口先完成，前端消费其合同；实现及验证由单一 worker 负责，独立 reviewer 审核。

### 1. 后端入口

- [x] 先核对 `gmap/http.py` 引用和当前未提交改动，复用 RPC 单页请求并增加基础首屏入口。
- [x] 更新 `maps_online_client.py`、`maps_online_schema.py`，接入预览和登录 options；复用既有限流、权益、用量与错误码。
- [x] 核对原 tasks 路由完整保持；不改任务表、任务执行、恢复和结算合同。

### 2. 网站流程

- [x] 更新 `HomePage.astro` 和首页采集组件、相关 i18n/schema；移除被替代的占位操作和仅本地计数逻辑。
- [x] 更新 `HistoryView.vue`、`scripts/dashboard/api.ts` 及侧栏字典，接入创建、约束、列表刷新和处理中轮询。
- [x] 接入首页与 Online 落地页 CTA，核对站级登录的当前语言 Dashboard 目的地和匿名数据丢弃。
- [x] 批准后把稳定产品规则落入 `014/feat.md`，同步 `015/feat.md` 的首页占位合同及 `015/tech-用户Dashboard.md` 的仅历史边界，详细接口规则以本方案为唯一来源；ROADMAP 只按实际交付更新状态。

### 3. 最少验证与交付

- [x] 一个匿名 API 主流程验证真实路由、首屏结果白名单、限流及无任务/文件/用量副作用；真实 MySQL/Redis，只有不可控 Google HTTP 出口允许用既有样本替换，同时核对仅 NID 和 offset=0 搜索路径。
- [x] 创建约束与正式提交优先扩展已有 Online 权益测试，覆盖匿名不可创建、Free 不得越权补全、options 与创建上限一致；已有完全对应的计量与下载证据不重复测试。
- [x] 网站聚焦 E2E 覆盖匿名提交与登录后丢弃、Dashboard 创建到历史和下载；用同一条浏览器流程验证移动端布局与桌面交互。补充零结果/请求失败后重试、额度耗尽和提交超限所需的最少断言，不跑全套网站测试。
- [x] 在真实 Google 和可用代理下调用一次匿名入口，记录端到端耗时、搜索页数及字段覆盖；没有拿到真实结果或预算超时不能记为性能验收通过。
- [x] 使用现有 seed 登录身份完成一次真实 Dashboard 提交 → Processing → Completed → CSV/ZIP 下载，核对历史与实际文件。网络/存储不可用单列未验收，不把出口替身结果记成完整公网闭环。
- [x] 后端受影响范围 Black、Ruff、mypy；`uv build`，启动后健康检查。网站 `pnpm build`，启动 preview 后核对首页与 Dashboard；端口冲突使用空闲端口。没有模型改动，不执行结构同步。
- [x] 文档检查限定新增和同步文档的格式、链接与内容一致性；记录通过、失败、环境 skip 和未执行，不以退出码替代必需证据。

## 实施验收记录

- 2026-09-07：已有采集优化基线为 15 个修改文件，+535/-85；实施期间其他执行者将其提交为 `dbbddb4`，本单元保留并复用粘性代理、共享连接与并发预算。首屏新增方法只抽取并调用 regular 单页步骤。
- 后端：`uv run pytest tests/integration/real/api/client/test_maps_preview_real.py`，1 passed；真实 MySQL/Redis、只读配置、仅 Google 出口使用原样本。覆盖白名单、3 行、限流、输入、无任务/计量副作用、登录与 options 上限。Black、Ruff、mypy 受影响范围通过，`uv build` 通过，应用启动及 `/api/system/health` 返回 healthy。
- 真实匿名预览：关键词 `coffee shop in Portland`，端到端 4.043865 秒；请求标识 `e00ece5615564130a661b9564efada70`，NID 与 regular 各一次，`pages=1 records=20`，返回 3 行。评论数为空，部分电话为空，未追加补全。
- 网站：`pnpm build` 通过；聚焦 `online-preview.spec.ts` 在 chromium、Mobile Chrome 两项目 2 passed，含失败重试、空结果、登录后丢弃、约束失败、超限、免费补全禁用、提交失败保留、创建轮询、完成用量及 ZIP、额度耗尽。证据位于 `website/tmp/online-preview-evidence/`。
- 真实闭环：`online-real.spec.ts`，chromium 1 passed；任务 `7dc2ebc2cfec48f999adeb25e452d066` 完成 58 条，真实 Google 与 R2，CSV 36 列，ZIP 内 CSV 与单项下载逐字节一致。证据位于 `website/tmp/online-real-evidence/`。此前精确商家关键词返回零条，非空结果断言失败，未计为通过。
- 测试脚本首轮因身份断言参数与 seed 参数错误失败，修正后通过；没有环境 skip。本地体验地址 `http://127.0.0.1:7661/`，API `http://127.0.0.1:7660`。稳定领域文档已同步。

- 独立审核共两轮、一次修复：首审两项 P2（提交与轮询竞争导致第一页刷新丢失、收起明细缓存过期）均在 `HistoryView.vue` 修复；同一 reviewer 复验“干的不错”，无未解决确认阻塞。新增 `online-preview.spec.ts` 的单条竞态回归，chromium 1 passed（9.8 秒），覆盖先在途列表、提交期间轮询与手动刷新、创建成功回第一页及完成后重新展开明细；证据 `website/tmp/online-refresh-evidence/`。修复后网站构建与启动通过，后端证据复用，未重跑无关测试。
- 限定文档检查：六份新增/同步文档的格式、文件链接与替代合同一致性检查通过；文档收尾没有重跑应用测试或构建。长期网络成功率、可信代理头与正式部署未验收。

## 规划记录

- 2026-09-06：已读取入口、Provider、站级登录、Dashboard 与领域合同。独立 reviewer 首审返回“干的不错”；hydra 随后确认匿名可看前 3 行基础预览，同一 reviewer 对更新后的方案复核仍返回“干的不错”。完整方案尚未获批，没有执行应用代码或测试改动。
- 文档验证：两份新增文档的末尾换行、行尾空白、7 个本地文件链接及展示合同一致性检查通过；本轮未运行构建、启动或应用测试。
