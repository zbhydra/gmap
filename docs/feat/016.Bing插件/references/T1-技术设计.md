# T1 · Bing 插件一期技术设计

> 一期(M2–M5)实施契约。竞品事实依据:`@../../research/bing-maps-scraper-竞品调研.md`(§2 架构 / §3 流程 / §4 DOM 适配 / §5 字段 / §6 协议 / §11 动态验证);工程规范:`spec-extension.md`;产品口径:`../feat.md`(已拍板差异 1–7)。本文档只写合同与边界,不放实现代码。

## 1 · 目录与文件责任(sites/bing)

```
src/sites/bing/
  config/
    contract.ts        # BingRemoteConfig 类型 + DEFAULT_BING_CONFIG 完整默认值(编译期)
    loader.ts          # 每 document 记忆化加载:background RPC → 失败回退包内默认(照 013 拍板机制)
    remoteFetch.ts     # background 持有 1h 缓存并按需 GET /api/client/bing/config(端点未上线时由 loader 回退)
  content/
    bootstrap.ts       # 站点自治 boot:配置加载 → 面板挂载 → 消息注册;被 src/content/index.ts 调用
    detector.ts        # 适配器版本探测(legacy/new)与容器/条目/加载态选择器解析
    collector.ts       # 采集状态机:检测轮询→循环(取 outerHTML→解析→去重→翻页)→完成;Stop
    parser.ts          # data-entity 双重 JSON.parse + 18 列行结构映射 + 评分兜底
    (rpcEvents.ts 实现期裁撤:面板与 collector 同处隔离 world 直连实例,无需本地消息通道)
    panel/             # Vue 直插面板(createApp 挂载到自建 fixed 容器;scoped CSS;design.md 主题变量)
    export/            # 18 列 CSV(BOM)/XLSX 组装与下载(复用底座既有导出工具形态,列定义本域自治)
    enrich/            # 二期(2026-09-02):types.ts(输入/结果契约)+ enrichApi.ts(POST /maps/enrich,requireAuth:false)+ enrichClient.ts(website 主机名去重、分批 ≤50、清 ###PRO### 占位后写回、enrich_complete 打点)
```

文件责任边界:不改 `src/core/**`(除 §4 声明的最小扩展:`background-register.ts`、`core/api/config.ts`,以及 U4 的 `core/stores/authStore.ts` 与既有 `core/utils/navigation.ts` 导航接线)、不改 popup 既有结构;locales 新增面板键组 `bingPanel.*`(14 语言文件同步,非英文英文基线)。

## 2 · 配置契约(contract 形状)

五组,全部可被服务端稀疏覆盖(Object.assign 浅覆盖,语义同 013 拍板):

- `adapters`:适配器版本数组,每项 `{ name: 'legacy'|'new', priority, detectors[], fallbackDetectors[], selectors: { listContainer[], listItems[], listItemsAlt[], scrollContainer[], searchAreaButton[], loadingIndicator[], openHours[], paginationButton?, infiniteScroll } }`。默认值 = 调研报告 §4.2 表格逐项(legacy: `.b_vList`/`.bm_oneMap` 体系 + `a.bm_rightChevron` 翻页,infiniteScroll=false;new: `.b_lstcards`/`#appShellRoot` 体系 + searchThisAreaButton 多候选,infiniteScroll=true)。
- `parse`:`dataEntityAttr='data-entity'`、评分来源开关(结构化字段 `entity.ratingValue` 优先,`infoboxHtml` 星级图类兜底)、去重键 = `entity.id`。
- `scrape`:检测轮询 1500ms、循环轮距 500ms、翻页后等待 2000ms、连续失败上限 12、列表就绪等待 8000ms/轮询 250ms、免费单次上限 20。
- `export`:文件名前缀 `Bing_Maps_Scraper_`、CSV 分隔符/换行/BOM 开关。
- `panel`:停靠 top 80px/right 40px、宽度 300–500px、示例搜索词 URL。

## 3 · 解析与行结构(18 列)

输出行字段与顺序以 `references/golden-samples/export.csv` 表头为唯一基线:ID、Name、Address、Featured image、Bing Maps URL、Latitude、Longitude、Rating、Rating Info、Category、Open Hours、Website、Phone、Emails、Social Medias、Facebook、Instagram、Twitter。

映射合同(来源 = data-entity JSON,字段清单见调研报告 §4.1):ID=`entity.id`;Name/Address/Website/Phone/Category=`entity.title/address/website/phone/primaryCategoryName`;Featured image=`entity.imageUrl`;经纬度=`routablePoint.latitude/longitude`;Bing Maps URL = `cp={lat}~{lon}&lvl=16.0&q={title, address}` 拼接;Rating/Rating Info 优先 `entity.ratingValue/ratingSourceName`,缺失走 infoboxHtml 星级类兜底;Open Hours 优先 `entity.openHoursText`,缺失走条目 DOM `.opHours`;Emails/Social Medias/Facebook/Instagram/Twitter 一期占位:免费行填 `###PRO###`,Pro 行留空(二期已于 2026-09-02 回填:免费保持占位,Pro 采集完成边沿自动补全,见域 changelog)。CSV 文件名 `{prefix}{N}_{timestamp}`,CSV 加 BOM。

## 4 · 底座最小扩展

1. `background-register.ts`:`recordMark` 的 `METHOD_TARGETS` 增补 `'content'`(content 采集/导出打点需要);一期不新增其他方法。二期(2026-09-02)唯一后续扩展:新增 `enrichBusinesses` RPC(仅 content 可调,请求/响应 128KB/256KB 上限,handler 接 `enrichApi.enrichBusinesses`)。
2. `core/api/config.ts`:新增端点常量 `BING_CONFIG: '/api/client/bing/config'` 与存储键 `BING_REMOTE_CONFIG`(形态对齐已删除的 013 MAPS 项;后端未上线时 loader 静默回退,不阻塞)。
3. `core/stores/authStore.ts`:U4 登录态/订阅态落点的最小扩展(复用底座既有账号 store,禁止另起域内重复实现)。

## 5 · 登录与门控(background)

- **登录(v3 浏览器身份,2026-09-01 起;协议合同见 `../../007.用户系统/tech-第三方登录.md` §9)**:popup 与面板未登录态 `Sign in` 两入口走同一 background RPC——PKCE verifier/challenge(S256)生成 → `chrome.identity.launchWebAuthFlow` 打开官网统一确认页 `/extension-login` → 用户复用 Google/邮箱登录并显式确认账号 → 一次性 code 经回调 URL fragment 回跳 → exchange 换插件独立 token → 快照比对后原子写 storage 三键;写入成功后 `invalidateSubscription()` + `refreshSubscription()` 强制重拉订阅态。原 v2 官网桥(`WebsiteAuthBridge` 整文件、`onMessageExternal`、manifest 固定 key 与 `externally_connectable`、`applyWebsiteToken`)已随迁移整体删除,manifest permissions 为 `['storage', 'identity']`。
- 门控:匿名(device_id)即免费;Pro 判定 = 既有订阅状态接口(007/006 域 client 已有),插件侧缓存时长已定(M4,2026-08-30):内存缓存 5 分钟 + 登录事件强制失效(v3 落地为登录写入成功后失效重拉;background 另有 auth 三键 storage watcher,登出时失效内存订阅态防门控陈旧;取值理由回写 feat 待决);免费:达 20 条停止 + 尾行提示 + Upgrade 引导;Pro:无上限。

- 订阅入口:面板徽标与达限升级按钮调用 `core/utils/navigation.ts` 的 `openPricingPage`，由 `buildPricingUrl` 集中组装网站地址与归因参数；URL 产品线合同见 [Pricing 页面入口](../../011.Pricing页/tech-pricing与自动续费.md)。插件仅消费权益状态，报价、购买、升级及管理交互由网站负责。

## 6 · E2E 基建(真实界面主验收)

单层真实界面设计(2026-09-02 hydra 拍板,替代原「离线 fixture 主验收 + 真实 smoke 辅助」双层,fixture 页与全部 route mock 已删除):

1. **真实 Bing 界面(唯一主验收)**:Playwright persistent context(`--load-extension=dist-real`,headful)打开真实 `www.bing.com/maps`,零 route mock、零本地 fixture 页,采集真实数据。断言链:面板注入 → 列表检测态 → Start → 匿名免费档 20 条截断(完成态 + 警告条 + Upgrade 引导)/ Pro 无上限 → 手动 Stop → CSV 导出(18 列表头 + 行数 + 末行提示)。
2. **反自动化身份**:去掉 Playwright 默认 `--enable-automation`、追加 `--disable-blink-features=AutomationControlled`,context 注入身份兜底 init script(website `scripts/playwright-browser-identity.mjs` 同款:webdriver 置否 + Client Hints brands 去 Headless);只消除已知自曝字段,不承诺绕过第三方 bot 检测。
3. **构建变体 dist-real**(`scripts/build-real.mjs`):API base 指向本地真实 backend(`127.0.0.1:7600`),SLS WebTracking 构建期禁用(真实 e2e 不污染生产日志库,打点失败由插件打点层容错);不跑 vue-tsc / zip(类型与产物校验由 `pnpm build` 覆盖)。
4. **登录态(Pro 用例,token 直注)**:登录流程不做 e2e(2026-09-02 hydra 拍板)。globalSetup 探测本地 backend 可达时 spawn `backend/scripts/e2e_seed_user.py --scenario bing-extension-pro`(幂等建号 + maps_extension Pro 订阅 30 天 + 与 `/auth/extension-login/exchange` 同构的 access/refresh token 对注册 Redis 白名单),spec 内经扩展 service worker 直写 chrome.storage auth 三键;auth/me 与 `subscription/status?product_line=maps_extension` 全走真实验证链。backend 不可达或 seed 失败 → 用例条件 skip,匿名用例不受影响。
5. **环境降级分界**:网络不可达 / 人机验证 / 落地域偏离(302 到 cn.bing.com 等)/ DOM 改版迹象 → `skipSmoke` 条件跳过并记 skip-reason 注解;插件自身行为(门控 / 计数 / 导出)失败照常 fail,不让环境波动掩盖产品缺陷,也不让上游改版打红日常门禁。

harness 要求:headful(扩展要求);每用例独立临时 profile;locale 固定 en-US(演示词结果形态稳定);产物断言读下载文件;构建前置 `pnpm build:real`;单命令 `pnpm test:e2e` 可重复执行。

## 7 · 单元拆分与依赖(执行循环调度图)

| 单元 | 目标 | 文件责任 | 依赖 |
| --- | --- | --- | --- |
| U1 | §6 e2e 基建(harness + fixture 页 + 冒烟) | `tests/e2e/**`、`playwright.config.ts`、`package.json`(playwright 依赖与 scripts)、`tests/e2e/fixtures/**` | 无 |
| U2 | §2 契约 + §3 解析器 + 单测 + 远程透传通道 | `src/sites/bing/config/**`(含 background 侧 remoteFetch)、`parser.ts`、`tests/unit/bing-*.spec.ts`、`tests/fixtures/**`(黄金样本转 fixture)、`core/api/config.ts`(§4.2)、`background-register.ts` + `BackgroundMessageRouter.ts` + `background/types.ts`(新增 `getBingConfig` 声明/handler/类型,架构师 2026-08-30 裁决:§2「经 background RPC」的落地四件套) | 无 |
| U3 | 采集状态机 + 面板 + 导出(§3 行结构落地) | `collector.ts`、`detector.ts`、`panel/**`、`export/**`、`bootstrap.ts`、`content/index.ts`、`locales/**`(bingPanel 键)、`background-register.ts`(§4.1) | U2 |
| U4 | §5 登录 + 门控 + 官网订阅入口 | `background/**`(openExtensionLogin RPC)、`core/stores/authStore.ts`(applyExtensionLogin + 订阅联动,复用底座账号 store)、`panel/**` 的权益徽标与网站入口、`core/utils/navigation.ts` 既有导航 helper | **U3(串行)**:入口接线与 U3 面板同域;Pro 判定消费 U3 collector 的停止条件读取点,U4 接入后放开无上限路径。(2026-09-01 登录部分已按 v3 重做,见 §5 与 007 plans/006) |
| U5 | 打点补齐 + 远程配置端点联调(视后端)+ 全链路 e2e 用例补齐 | 各处小改 + `tests/e2e/*.spec.ts` 补用例 | U1–U4 |

汇合后整体 reviewer:跨单元契约(RPC/存储键/消息常量/locales 键)、组合行为、遗漏对照 feat.md 验收标准。

## 8 · 验证命令(每单元 worker 必跑)

`pnpm check`(type-check+lint+format+permissions)、`pnpm test:unit:run`、`pnpm build`;U1 起加 `pnpm test:e2e`;禁止引入新运行时依赖(U1 的 @playwright/test 为 devDependency,属已批准基建)。
