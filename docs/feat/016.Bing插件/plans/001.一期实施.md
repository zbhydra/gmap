# 001 · Bing 插件一期实施

> 任务跟踪 + 验收记录。重要决策回写 `feat.md`,本文档不留正式结论。
> ⚠️ 登录部分(M4「登录接收」)已被 007 域 plans/006(extension 登录 v3 · browser identity,2026-09-01 实施)替代:v2 官网桥(onMessageExternal + website `/extension-login-bing` 桥页)已删除,现行登录为插件发起的 v3 流程(popup + 面板双入口),合同见 `../../007.用户系统/tech-第三方登录.md` §9 与 `@references/T1-技术设计.md` §5。其余章节仍为一期历史记录。

## 目标

交付一期完整功能(B1–B7),达到 feat.md 域级验收标准 1–6。

## 非目标

Email/社媒真实挖掘、增长件、上架提交(见 feat.md 不包含)。

## 阶段与任务

### M1 ✅ 工程底座(2026-08-30 完成)

- [x] `extension-bing/` 建 目录,复制 `extension/` 底座;node_modules 重装
- [x] gmap 站点层清理(sites/maps、injected、maps 测试、mapsPanel 文案、getMapsConfig RPC、MAPS 端点/存储键)
- [x] tg 残留清理(TG_COLORS、tg 端点、tg 反馈群字段、商店描述、扁平键文案、SLS 项目名占位 bingmaps)
- [x] manifest Bing 化(`https://www.bing.com/maps*` + document_end;固定扩展 ID key;官网桥白名单 localhost)
- [x] website:`/extension-login-bing` + bing 扩展 ID 常量 + 14 语言文案 + sitemap 屏蔽
- [x] 黄金样本入域 `references/golden-samples/`(data-entity 样本 + 竞品实测导出 CSV)
- [x] 验收:pnpm check 全绿、单测 124 通过、构建通过、产物 key 推导 ID 与 website 配置一致

### M1.5 e2e 基建(U1,先行)

- [ ] 技术契约:`@references/T1-技术设计.md` §6——离线 fixture 层(黄金样本重建的 Bing Maps 模拟页)+ 真实 Bing smoke(允许失败)
- [ ] Playwright persistent context 加载构建产物扩展;`pnpm test:e2e` 单命令可重复
- 验收:对当前中性骨架跑通「扩展加载 + 打开 fixture 页 + 面板未挂载的页面健康断言」;全程无人为干预

### M2 sites/bing 配置契约 + 解析

- [ ] `sites/bing/config/contract.ts`:适配器(legacy/new 探测器与候选选择器)、解析 schema、节奏参数(默认值依据调研报告 §3/§4);远程稀疏覆盖通道沿用底座机制
- [ ] `sites/bing/content/parser`:data-entity 双重解析(entity + routablePoint)、评分/营业时间(优先结构化字段,气泡解析兜底)、去重键
- [ ] 黄金样本(`references/golden-samples/`)入 `tests/fixtures/`,解析单测
- 技术契约:T1 §1/§2/§3
- 验收:单测全绿;18 列字段与竞品实测导出(`references/golden-samples/export.csv`)逐列对照一致

### M3 采集循环 + 面板 + 导出(U3)

- [ ] 采集状态机:检测轮询(1.5s)→ 采集循环(500ms 轮距、滚动/翻页、2s 翻页等待、12 次失败上限)→ 完成;Stop
- [ ] 面板(Vue 直插,scoped CSS,design.md 主题):三态 + 元素按 feat.md 界面表;文案入 vue-i18n(14 语言键,非英文先英文基线待补翻)
- [ ] 导出:CSV(BOM)/XLSX,免费 20 条截断 + 末行提示;文件名 `Bing_Maps_Scraper_{N}_`
- 验收:真实搜索全链路(验收标准 2);免费 20 条停止 + 尾行提示 + Upgrade 按钮存在(跳转目标与 Pro 判定在 M4 联调)

### M4 登录接收 + 门控(U4)

> ⚠️ 本节登录接收(onMessageExternal 桥)已按上方头部标注被 v3 替代;门控与 Pricing 部分仍有效。

- [ ] background `onMessageExternal`:校验 sender.origin(官网域)→ 校验并存储 token → 账号/订阅态;LOGIN_RETURN 关 tab
- [ ] 免费/Pro 判定:订阅状态接口接入 + 缓存(时长 M4 定);匿名 device_id 免费路径
- [ ] Pricing 信息页(对比表 + 订阅跳转官网)
- 验收:验收标准 3、4

### M5 收尾(U5)

- [ ] 打点补齐(search/export_results 含维度);content_open 确认
- [ ] 远程配置端点接入(视后端排期:机制通道 M2 已就绪,常态用本地默认;接入后补验收标准 5 后半)
- [ ] 构建产物复核(zip、商店形态、权限最小化)
- 验收:验收标准 5、6

## 技术契约

- 单元拆分/文件责任/验证命令:`@references/T1-技术设计.md` §7/§8(执行循环按此调度)

## 依赖与风险

- 后端订阅状态接口对插件客户端可用性(006/007 域已有,M4 首日验证)
- Bing DOM 改版风险:黄金样本 2026-08-30 实测有效;改版由远程配置热修兜底
- 官网生产域名未定:官网 base URL 走构建期配置,dev 先 localhost,M4/上架前补(登录 v3 后无 `externally_connectable` 白名单,仅剩 base URL 待定)

## 验收记录

- 2026-08-30 全阶段完成:U1–U5 逐单元 worker/reviewer 循环全部「干的不错」(U1 2 findings/U2 1/U3 0/U4 2/U5 1+跨单元类型根治);整体跨单元审查另见执行循环最终汇报。基线:pnpm check 全绿(测试类型全覆盖 0 错误)、单测 182/182、build 通过、e2e 10 passed + 1 条件化 skip(真实 Bing smoke:本机 cn.bing.com 地域重定向,海外网络可达时实跑)。feat 域级验收:1✅(黄金样本单测) 2✅(fixture 全链路+18 列逐列对照) 3✅(免费/Pro 双路径 e2e) 4✅(登录同步/匿名路径 e2e) 5◐(通道就绪+回退已验;端点接入视后端) 6✅。
