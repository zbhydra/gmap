# 004 · 同步支付系统：计费模型与 ClinkBill

> 状态：待用户批准，禁止实施。
>
> 上游：extension-tg-download 2026-08-29（gmap 分叉点）以来的支付系统提交。实施基线是
> `e5019200..HEAD` 中支付相关文件的全量 diff（即上游 HEAD 最终状态），以下 12 个提交仅为索引：
> `2d16f48e` ClinkBill 接入、`3ab64c56` Clink 回跳修复、`93c1eddb` 支付回跳与 Pricing 展示、
> `273c4aab` 统一计费周期与实例模型、`9c145cbd` 收敛商品单一计费模式、`12888fd4` Clink 未处理事件、
> `8bc45471` 自动续费渠道由配置决定、`2c5d0aca` Stars 自动续费账期、`ea187fc6` 统一 expires_at 判账期、
> `99bc90de` 自然月写空、`7d7b8450` 旧时长字段保留回滚、`84729b8e` 删除旧时长字段删除断言。
> 已确认决策：含 ClinkBill；保留 gmap 单用户多产品线订阅（(user_id, product_line) 复合主键）。

## 目标

gmap 支付系统与上游对齐：商品单一计费模式 + 订阅实例账期（自然月/渠道归一）+ ClinkBill 渠道 + 站内取消事实透出，并把契约变更同步到网站与两个插件端。

## 非目标

- 不同步上游登录功能（extension-pro v3）、DOM 巡检、admin 积分标签页、部署分支脚本。
- 不引入 extension-pro / website-tgd-pro 端（gmap 无此二端）。
- 不配置 ClinkBill 密钥/渠道行（实际启用由 `config_payment_channel` 配置决定，代码就绪即可）。
- 不新增站内取消 API（上游本次也只有状态透出 + 运维脚本）。

## 用户可见行为变化

1. 一次性支付订阅到期 = 当前账期起算自然月（month/quarter/year），不再是配置天数累加；好评赠送按天加时保留不变。
2. 每个商品只有一种计费模式（商品列 auto_renew）；渠道价 auto_renew_supported 决定该渠道能否卖自动续费模式。
3. 下单请求新增必传校验：auto_renew + period 必须与商品配置一致，不一致返回价格已更新。
4. 订阅状态新增 cancel_at_period_end / cancel_available / cancelled_at / payment_method，period 来自订阅实例。
5. 新增 ClinkBill 托管结账渠道（配置启用后出现在收银台）。

## 改动范围与文件责任

### U1 后端计费模型 + ClinkBill（一个可验收单元，先于其他单元）

- `constants/payment.py`：CLINK 标识、`payment_currency_matches_channel`、`recurring_provider_sku_parts`（PayPal Plan ID / Clink `productId:priceId` 冒号约定）。
- `constants/subscription.py`：周期枚举 FREE→NONE、新增 QUARTER/YEAR；`from_metadata` 去掉 period 参数（free 判断只看 product_id），四个调用方同步适配——`subscription_client.py`、`subscription_service.py`、`subscription_status_service.py`、`usage_service.py`（gmap 独有的多产品线配额服务，对应上游 quota_service 的适配角色）；metadata 移除 auto_renew（gmap 版保留 monthly_quota 与产品线常量）。
- `constants/order.py`：`OrderCheckProductParam` 增 auto_renew/period。
- `models/subscription_model.py(+.pyi)`：复合主键行内新增 product_price_id、auto_renew、period、payment_method、original_order_no、channel_subscription_id、channel_uid、start_at、cancelled_at；expires_at 移到列序最后（upsert 时间真相最后写）。
- `models/config_subscription_product_model.py`：period String(16) default 'none'、新增 auto_renew 列、duration_days 标注废弃保留（gmap 版保留 product_line 与线级唯一约束）。
- `models/config_subscription_product_price_model.py`：新增 auto_renew_supported、价格行 id 透出；table info 声明旧列删除（schema_sync_drop_columns）。
- `utils/time.py`：`add_natural_months`（业务时区自然月，缺日取月末）。
- `services/payment_config_service.py`：checkout 配置校验 auto_renew+period 与商品一致；`_is_price_available`（币种渠道匹配 + auto_renew_supported）；方案列表只出 month/quarter/year；展示价校验前置。
- `services/config_subscription_product_service.py` / `config_subscription_product_price_service.py`：行数据类切新字段（gmap 版保留 product_line；上游去 @singleton 一并跟随）。
- `services/subscription_service.py`：履约双路径——一次性按快照 period 自然月续期并覆盖实例事实；自动续费只按 Provider 归一化到期时间推进（advances 守卫，实例字段 case 保护）；`extend_subscription_days` 保留（好评赠送/加时）。快照 JSON 保留 gmap 特有 product_line 字段用于定位行，其余切上游新口径（product_price_id/period 等）。
- `services/subscription_status_service.py`：实例透出（period 取行内值回退、auto_renew 计算、cancel 三态、payment_method）；gmap 按 product_line 组装保持不变。
- `services/payment_service.py` / `provider/payment/payment_base.py`：Clink 注册、PaymentRequest 增 user_id。
- `api/callback/paypal_callback.py`：`PAYPAL_PAYMENT_METHOD` import 从 provider 模块切到 constants（随 constants 收敛）。
- `provider/payment/paypal.py`：sale completed 改异步并查订阅状态，provider_subscription（channel_subscription_id/original_order_no/start_at/expires_at）入回调 metadata；币种与 SKU 校验走 constants 助手。
- `provider/payment/tg_star.py`：recurring successful_payment 补 provider_subscription（ Stars 账期）。
- `provider/payment/clink.py`（新）+ `api/callback/clink_callback.py`（新）+ `main.py` 注册：Hosted Checkout、HMAC 验签、order.succeeded/invoice.paid 映射、未处理事件确认接收、原始报文日志。
- `schemas/order_schema.py` / `schemas/subscription_schema.py` / `api/client/order_client.py` / `api/client/subscription_client.py`：下单请求 auto_renew+period；方案序列化切 auto_renew（商品级）+ product_price_id、去 provider_sku/duration_days（gmap 版保留 product_line/monthly_quota 输出）；状态响应新增取消字段。
- `init/sync_database_schema.py`：removed 列支持（模型 table info 声明）、旧唯一键前缀/扩展检测、价格重复数据守卫、先补列后删列顺序（gmap 版保留复合主键迁移逻辑）。
- `scripts/seed_subscription_products.py`：auto_renew 移到商品列、free 档 period 'none'、duration_days 保留数值标注废弃。
- 既有运维脚本 `scripts/_order_auto_renew.py` / `cancel_order_auto_renew.py` / `query_order_auto_renew.py`（分叉时已带入，上游本次未改）：回归验证在 paypal/clink 重构后仍可运行。
- `docs/feat/006.订阅系统/tech-订阅商品与状态.md`、`docs/feat/004.订单系统/tech-ClinkBill支付.md`、`tech-支付与履约.md`、`tech-订单数据与状态机.md`：随上游实现同步合同与状态机结论（删 gmap 分叉时拷来的 ClinkBill 旧方案 plan 与暂缓的取消续费 plan 中已被实现覆盖的部分）。

### U2 后端测试移植

- 对齐上游新增/修改：clink callback api/real、clink payment service、subscription service（履约双路径）、order api（下单新参数）、checkout config api、sync_database_schema（removed/守卫）、paypal/telegram service（provider_subscription）、time（自然月）、order recurring real。
- 测试布局按 gmap 现状（tests/test_server 与 tests/integration/real）适配；先跑通 U1 全量。

### U3 网站前端

- `src/components/order-checkout/`：类型与请求体增 autoRenew/period、PaymentOption 价格与说明文案、initialPaymentMethod 恢复、checkoutUrl 读取 + clink 域名白名单、clink 渠道图标。
- `src/components/credit-purchase/paypal-return.ts`：泛化为 payment return（provider 参数、DOM 文案钩子），保留 paypal 入口；`credit-checkout.ts` 收敛复用 order-checkout 查询/分类函数。
- `src/components/pricing/`：单一计费模式展示与下单参数（在 gmap 产品线/月度额度版本上重放上游支付相关改动，不整文件覆盖）。
- 新增 Clink 回跳：`src/components/pages/ClinkReturnPage.astro`、`src/pages/clink/success.astro`、`cancel.astro`、`src/i18n/clink-return.ts`、`public/payment-icons/clinkbill.png`；sitemap 增页。
- `src/i18n/pricing.ts` 与 PricingPage/PricingPageShell 文案跟随。

### U4 插件端契约

- `extension/src/core/api/subscription/`：status 响应收窄解析（period 增 quarter/year、auto_renew/extension_download 必填、去 one_time）+ 单测。
- `extension-bing`：period 联合类型增 quarter/year（bing 版契约只消费 period，最小改动）。

### U5 文档与网站测试收尾

- `website/tests/module-scripts.test.js`、`website/e2e/website.spec.ts`：跟随上游断言（该文件当前有他人在途改动，实施时基于工作区现状叠加）。
- `docs/feat/006.订阅系统/changelog.md`、`004.订单系统/changelog.md`、`011.Pricing页/changelog.md`、`docs/ROADMAP.md` 状态更新；`docs/feat/011.Pricing页` 相关 feat/tech 文案核对。

## 执行顺序与依赖

U1 →（U2、U3、U4 可并行）→ U5。U1 完成前前端联调无意义。

## 验收标准

1. 后端：全量 pytest 通过；black/ruff/mypy/compileall 干净；`sync_database_schema` 先 dry-run 审查再执行，`user_subscriptions` 新列就位且旧行不破坏；后端按 business 角色启动无异常。
2. 未配置 clink 渠道时，现有 PayPal / TG Stars 下单、回调、履约、状态行为与同步前兼容（存量订单照常履约）。
3. 配置 clink 沙箱渠道后，ClinkBill 下单跳转、Webhook 验签履约、一次性与自动续费路径测试通过（无真实密钥时以签名构造测试为准，不宣称真实首笔支付）。
4. 网站构建 + 启动；定价页按商品单一计费模式渲染，下单请求携带 auto_renew+period，Clink 回跳页可达。
5. 两个插件端 pnpm check / unit 通过；订阅门控对 quarter/year 正确放行。
6. gmap 多产品线回归：extension 与 maps_extension 线各自购买/续期/状态互不干扰；好评赠送按天加时不受自然月切换影响。

## 已证实风险与未知

- 工作区有并行的他人在途改动（22 个文件，含 `website/tests/module-scripts.test.js` 与 `docs/feat/004.订单系统/changelog.md` 与本任务交叠）：实施基于工作区现状叠加，不回退、不代提交他人改动。
- 存量订阅行新增实例列为 NULL：状态 period 回退与档位回退逻辑已覆盖（上游同款），无数据回填需求。
- 自然月与 30 天口径差异使长账期到期日略有偏移：上游已拍板口径，跟随。

## 验证命令

```bash
cd backend
uv run pytest tests -x -q            # 全量，先 collect-only 冒烟
uv run black --check src/app scripts tests
uv run ruff check src/app scripts tests
uv run mypy src/app
uv run python -m compileall src/app
uv run python -m app.init.sync_database_schema --dry-run   # 审查后去掉 --dry-run 执行
# website: pnpm build && pnpm test；extension / extension-bing: pnpm check / unit
```
