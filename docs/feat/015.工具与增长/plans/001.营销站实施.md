# 001 · 营销站实施(执行计划)

> MapsGrab 营销站(website)W1–W7 执行单元。依据:`@../feat.md`(需求合同,含 2026-08-30 hydra 三裁决:Online 入口留空点击无效 / 套餐与法务参考竞品 / 工具矩阵全做)、竞品官网抓取材料(`@../references/D1-免费工具矩阵.md`、`D2-文章矩阵与产品矩阵.md`)。
> 工程形态:**新子项目 `website-mapsgrab/`**,从 `website/` 复制改造(参照 extension/ → Maps 插件的改造模式);本地开发测试,域名后配(hydra 2026-08-30)。**2026-08-31 后续:TG 主站退役删除,本子项目更名 `website/` 并接管主站端口 7620**(本文历史小节的 `website-mapsgrab`/9630/7630 均为当时实况,不回改)。
> 状态机:`pending → ready → running → review → fix → done`;`blocked` = 需用户决策。

## 0 · 站点技术决策

- **工程**:Astro 5(复制 `website/` 工程)。**注意:tech-website.md 与 013 时代对该工程的描述已过时**——当前 website/ 实际不存在下载器/mediabunny/TG 平台落地页;真实构成以 W1 开工时的目录清点为准(§1 W1 清单)。
- **本地 dev 端口**:`9630`(全仓零占用,已核验);构建产物独立目录。
- **语言**:EN 首批;保留 010 多语言架构,后续语言按需加。
- **UI**:全量消费 design.md/design.dark.md token(Material You,亮暗双主题);品牌 MapsGrab。
- **Online 占位**:导航与首页产品卡保留 Online 形态,`href` 不指向功能页(点击无效,不 404)。
- **e2e**:Playwright(沿用 website/ 既有 playwright 底座与 E2E_WEB_PORT 模式),断言页面渲染/导航/工具功能/法务链接;天然全自动零人工。

## 1 · 执行单元

| # | 单元 | 范围 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| W1 | 工程底座 | 复制 `website/` → `website-mapsgrab/` 并按三类清单改造:**保留改造**(components/auth、components/pricing、credit-purchase、order-checkout、pages/paypal——W5 购买链路基座;scripts/homepage 全套运行时;Layout/sitemap/i18n 架构);**剥离**(TG changelog 页、TG GA4 ID `G-LBSKJD0H16` 与 JSON-LD/twitter:site 的 TG 身份、cloudflare retired-redirects 的 TG 条目、deploy conf 的 TG 域名、lib/contact 与 XBrandIcon 的 TG 常量、public/llms*.txt);**替换为占位**(astro.config site、GA4 槽位清空待 W6、支持联系方式占位)。astro.config `site` 占位、dev 端口 9630、build(含 astro check)通过、基础 Layout + 空 home;**e2e/测试底座适配**(playwright.config 默认端口 9630、剥离 TG e2e spec 与 global-setup seed、tests/module-scripts.test.js 清理或重写、E2E_WEB_PORT 机制保留);README 待回填清单补全(样式 token 化/favicon) | 无 | ✅ done(2026-08-30,reviewer 3 低危(F1 口径快照不计、F2 README 补 2 项已修、F3 环境特性),build 8 页/dev 9630/module-scripts 46+46/playwright 35+1 四 project/TG 残留零命中) |
| W2 | 首页 + 插件产品页 + 下载页 | 首页五要素(hero / 三产品卡(插件✓ Online 占位 / API 占位) / 社证(用户评价占位区) / FAQ / CTA 带 utm);产品页(功能清单对齐 A5 36 列与竞品宣传 + **版本说明段**承接 changelog 职能);下载页(Edge/Firefox 商店链接占位 + 直装 zip 说明 + 安装引导);埋点:data-cta 归因属性(事件通道 W6 接) | W1 | ✅ done(2026-08-30,reviewer 4 finding(导航缺 Online 占位/Plus Code 虚报/日期矛盾/死变量)修复复验通过;build 10 页、e2e 66 passed、Lighthouse 100×4) |
| W3 | 法务页 + About/Contact | 隐私政策 / Terms(结构参考竞品两页,内容 = MapsGrab 事实:公开商家数据采集、本地 storage、自有后端四类用途〔配置/配额/enrich/使用记录〕、可选 Drive/HubSpot 用户授权、无广告/第三方追踪——与插件 privacy.html 口径一致并扩写)、About(品牌/边界)、Contact(支持联系方式占位) | W1 | ✅ done(与 W2 同 worker 同轮) |
| W4 | 工具矩阵 ×7(**交互形态已钉死**) | ①Place Id Finder:粘贴完整 Maps 链接(URL 含 `@` 或 `data=`,**短链 maps.app.goo.gl 不支持**——跨域展开与纯前端冲突)→ 纯前端提取 Place ID/CID/FID(复用 013 解析规则);②Review Link Generator:粘贴完整 Maps 链接 → 构造 `search.google.com/local/reviews` 直链;③Email Checker:**纯格式校验**(RFC 形态 + 常见误报,SMTP 校验不做——纯前端约束);④LatLong→DMS;⑤DMS→DD;⑥Bulk Keywords Generator(关键词 × 地点组合生成);⑦Merge CSV(纯前端合并)。每工具一页:表单 + 纯前端逻辑 + SEO 正文 + CTA(data-cta);埋点:工具页进入、功能使用次数(data-* 属性,事件通道 W6 接) | W1 | ✅ done(2026-08-31,reviewer 2 低危(sharp 死依赖移除/ga4.spec 类型越界按 spec-website §2 重写)修复复验通过;build 17 页、e2e 158 passed、Lighthouse 全页 ≥95) |
| W5 | Pricing 展示 + 006 扩展 + 购买链路 | 套餐页展示对齐竞品插件三档(Free 1,000 / Pro $39 100,000 / Business $99 500,000 records/月;Online/API 卡片占位不可购);**006 扩展**:产品线维度 + Maps 三档定义 + 购买链路接 004 支付 + 额度映射(U7 maps_quota total 从用户套餐读);**含 013 U7 回归验证**(免费档行为不变:无套餐用户仍 1000/月 + 门控);**联动登记范围(2026-08-30 架构师裁决)**:本单元仅输出 pricingUrl 常量于 backend `/maps/config` operations(已落地);插件端「订阅跳转按钮」消费归 W7 | W1;006 扩展触发已批准闸门(C2 拍板);模型改动跑 sync_database_schema | **running(2026-08-30,与 W4 同 worker;持 006/004 backend 与 pricing 键段)** |
| W6 | SEO + GA4 + Lighthouse 收尾 | sitemap/llms.txt/robots 重写为 MapsGrab 事实、GSC 占位、meta/OG 全页核对、GA4 接入(hydra 提供 measurement_id,或占位集中一处待换)、埋点事件通道接入(data-cta/进入/功能使用次数 → GA4)、**Lighthouse 全页 ≥ 90**(perf/a11y/seo,不达标即修) | W2–W5 | ✅ done(2026-08-31,reviewer 2 低危(sharp 死依赖、ga4.spec 类型)修复复验通过;Lighthouse 15 内容页 ≥95、GA4 双态 e2e、sitemap/llms/robots/meta 逐页核对) |
| W7 | 插件联动(小单元,extension/ 端) | 013 U7 遗留接线:插件「额度用尽/升级」入口指向 MapsGrab Pricing(域名占位常量);含 U7 门控回归(免费档不变) | W5 | ✅ done(2026-08-31,reviewer 2 finding(可见性单一事实源/place 模式补测)修复复验通过;unit 326 passed、e2e 19 passed) |

## 2 · 依赖与并行

- 串行:W1 → {W2, W3, W4, W5} → {W6, W7}。
- 并行:W1 完成后最多 2 worker——分组 W2+W3、W4+W5(W4/W5 各自页面与逻辑文件独立)。
- **契约文件互斥**(同 013 模式):`i18n/lang/en-US.ts` 文案字典、`i18n/schema.ts`、导航/页脚数据文件为共享契约——同一时间只允许一个单元修改;各单元页面内容文件独立。需要扩契约时向架构师申请调度。sitemap 与 ui.ts 的一致性约束(tech-website)在 W6 统一核对。

## 3 · 通用验收与验证命令(每单元)

- `pnpm build`(含 astro check)通过;新增逻辑直接覆盖验收行为的最少测试;e2e 相关 spec 全绿且全自动。
- W2/W4/W6 加:Lighthouse 关键页 ≥ 90(perf/a11y/seo,W6 起全页核对)。
- W5 backend/006 部分:同 U7 标准(pytest/black/ruff/mypy + sync_database_schema 如动模型)。
- W7(extension/ 端):按 013 计划 §3 那套——`pnpm build`(含 vue-tsc)、`pnpm lint` 零警告、`pnpm test:unit:run`、相关 e2e。
- 零第三方运行时依赖(工具纯前端),**唯一豁免 GA4 gtag(W6)**;UI 消费 design token;文案 i18n(en 基线);硬约束(GET/POST、中文注释);TS 类型边界按 spec-website §2:禁 any,`as unknown` 仅限 JSON.parse 后 + 类型守卫收窄。

## 4 · 已批准基线(不重问)

1. 不强制登录;2. Online 入口留空点击无效;3. 套餐参考竞品分产品订阅(C2 裁决,2026-08-30);4. 工具矩阵全做;5. 法务参考竞品口径改写 MapsGrab 事实;6. 域名后配、本地开发测试;7. 品牌 MapsGrab + UI token 合同;8. SheetJS 不入本站(工具矩阵无 XLSX 需求,Merge CSV 自实现)。

## 5 · 待决(不阻塞 W1)

- Pricing 购买渠道:004 现有渠道(ClinkBill 等)是否满足竞品对标的 Stripe/Paddle 形态,W5 设计时定。
- License Generator 工具:与付费体系直连,后置(基线既有)。
- Guides/对比文章内容生产:阶段 4 运营,不在本计划。
- GA4 measurement_id:hydra 提供(W6 前即可)。

## 6 · 状态日志

- 2026-08-30 计划创建。W1 ready,其余 pending。
- 2026-08-30 W4+W5 done:工具矩阵 ×7(纯前端,placeId 换算移植/RFC4180 自实现/Lighthouse ≥90)+ Pricing 三档展示 + 006 产品线扩展(复合主键迁移文档化 + 播种脚本 + 验价改造 + 额度映射含 013 免费档回归)+ PayPal 回跳按产品线文案。worker-reviewer 一轮 6 finding(F1 高:PK 迁移无文档;F2 高:播种无脚本;F3 回跳文案矛盾;F4/F5 低;F6 裁决记录)修复复验通过(2 低危备注留档:播种 price 行断言/真实 SKU 覆盖提示)。验证:backend 535 passed、website-mapsgrab build 17 页、module-scripts 45/45、e2e 130 passed/2 skipped、Lighthouse ≥90。
- 2026-08-30 计划复验:8 项裁决核验通过,补修 5 项簿记(N1 W1 补 e2e/测试底座适配;N2 W4 短链不支持钉死;N3 工具页进入/使用次数埋点落 W4/W6;N4 §3 GA4 豁免与类型边界措辞;N5 W7 验收命令按 extension 套 + 日志笔误)。复验通过后计划进入执行。
- 2026-08-30 W1 done:website-mapsgrab 底座(三类清单改造 8 页构建/dev 9630/playwright 四 project 35+1/TG 残留零命中);reviewer 3 低危(F1 口径快照不计、F2 README 待回填补 2 项)。
- 2026-08-31 W4+W5/W6/W7 done:工具矩阵 ×7、Pricing 三档 + 006 产品线扩展(复合主键迁移文档化 + 播种脚本 + 硬断言)、GA4/SEO/Lighthouse(15 页 ≥95)、插件升级按钮接线。全部单元 worker-reviewer 循环闭环,提交整体汇合审查。
- 2026-08-30 W2+W3 done:首页五要素/产品页/下载页/隐私与 Terms/About/Contact。worker-reviewer 一轮 4 finding(导航缺 Online 占位、Plus Code 虚报、日期矛盾、死变量)修复复验通过。验证:build 10 页、e2e 66 passed/2 skipped、module-scripts 46/46、Lighthouse 首页与 /extension 100×4。
- 2026-08-30 计划审查修复 8 项 finding:F1 W1 剥离清单按 website/ 实测重写(保留 auth/pricing/order/paypal 为 W5 基座,剥离 TG 身份资产);F2 Email Checker 钉死纯格式校验;F3 Place Id Finder/Review Link Generator 钉死 URL 解析式(无 API key);F4 GA4/埋点/Lighthouse 落 W6 与 §3;F5 契约文件互斥机制;F6 开 W7 插件联动单元(013 U7 遗留 + 013 回归验证);F7 删 lint 行(该工程无 ESLint,astro check 已含);F8 标题 W1–W7 对齐 + 社证/版本说明补。
