# 用户 Dashboard 实施清单

> 目标与边界见 [Dashboard 方案](../tech-用户Dashboard.md)。已获批准并实施（2026-09-06）。

## 1. 全站认证与工作区骨架

- [x] 在 `website/package.json`、`pnpm-lock.yaml`、`astro.config.mjs` 接入兼容 Astro 5 的 Vue 官方集成及 Vue 类型检查；同步 `docs/references/specs/spec-website.md` 与 `website/README.md` 的组件、状态和构建边界。（@astrojs/vue 5.1.4 + vue 3.5.42 + vue-tsc 3.3.11；build = astro check → vue-tsc --noEmit → astro build）
- [x] 核对登录控制器、事件、全局对象及 Pricing 待购引用；将 `PricingAuthModal.astro`、`pricing-auth-controller.ts` 的公共能力迁到 `components/auth/`，修改 Pricing 必要调用方，删除旧包装。（事件更名 site-auth:*；`/extension-login/` 传 `includeSiteAuth=false` 保留桥接 owner，不重复消费 Google 回跳）
- [x] 对本轮触及的现有界面按方案的迁移判据处理；确需 Vue 重构时连同必要调用方一起迁移，删除旧 DOM 控制器，复用已有验证路径，不扩大业务功能。（渠道指引弹窗随渠道管理迁为 Vue 版；Pricing 账户区与渠道管理入口删除，Pricing 保留 tab 切换、购买与升级）
- [x] 修改 `website/src/layouts/Layout.astro`，接入导航登录态和工作区模式；在 `components/dashboard/` 增加 Vue 工作区根组件、侧栏和业务视图，在 `pages/dashboard/` 及语言路由以 `client:load` 装配三个页面；只提取本轮涉及的站级 Astro 组件。（workspaceMode 隐藏营销导航与 footer；root + [lang] 双路由）
- [x] 接入个人信息、退出、直接访问认证和移动端菜单；保留插件登录桥独立流程。（会话恢复/不可达重试/401 统一失效；移动端抽屉侧栏）
- [x] 在 `website/src/i18n/schema.ts` 与现有字典加入文案，在 `src/sitemap/languageSitemap.mjs` 排除工作区 URL。（DashboardContent + nav 登录/Dashboard 键；noindex + sitemap 三路径排除）

## 2. 接入现有业务

依赖第 1 阶段。

- [x] 在 Dashboard 历史页面接现有列表、详情和下载接口，按方案处理分页、空态、错误与手动刷新；二进制下载复用 API 请求头边界。（每页 20、ET 时区、ZIP 经 blob 临时 URL；401 汇入站级会话失效）
- [x] 在订阅页消费三线摘要，将 `PricingPageShell.astro`、`pricing-page-controller.ts` 的渠道管理及 `PricingCancellationGuideModal.astro` 迁入订阅管理；Pricing 保留选购和升级能力。
- [x] API 管理展示未开放状态；核对 `components/pages/LandingPage.astro` 与营销内容配置中获取 Key 类 CTA，统一指向 API 管理页。（API 四页 hero/CTA 按钮改真链接，Online 页保留占位）
- [x] 同步 `015/feat.md` 的入口归属、`011/tech-pricing与自动续费.md` 的账户管理归属及相关 i18n 文案；ROADMAP 只登记实际交付状态。保持首页采集入口原范围。

## 3. 最少验证

- [x] `cd website && pnpm build`，确认包括 Vue 单文件组件类型检查，随后 `pnpm dev -- --host 127.0.0.1 --port <空闲端口>` 启动检查；运行 `python3 scripts/ui_token_lint.py` 检查本轮样式，并核对它覆盖新增 Vue 文件。（lint 扫描 .vue 生效；本轮新文件违规已清零，存量 `ClinkReturnPage.astro` 4 处为基线遗留，另报）
- [x] 更新 `website/e2e/website.spec.ts` 的现有 Pricing 购买路径，覆盖登录后继续购买、升级与迁移后的渠道管理；只运行对应测试，不跑整个套件。（渠道管理断言随迁移移至 dashboard.spec；Pricing 主路径 chromium 通过）
- [x] 一组聚焦 Dashboard 的 Playwright 用例覆盖：匿名登录与取消、登录后导航与直达子页、退出回主页；历史分页与明细、CSV/ZIP 下载及无文件反馈；三线免费/有效/不可用状态、Pricing tab 跳转与 API 未开放。复用浏览器身份注册规则。（e2e/dashboard.spec.ts 5 用例，chromium + Mobile Chrome 全过）
- [x] 桌面及移动视口各检查一次截图、菜单与个人信息可达、长邮箱换行、焦点和无重叠。补充检查工作区 HTML 无私人数据且 noindex、sitemap 不收录。（截图存 Playwright 产物；修复移动端历史表格撑破视口的真实布局缺陷）
- [x] 用已有真实登录态和 Online 任务完成一次历史查询与下载冒烟。（**已有本地后端+DB+本地存储替身下载闭环通过**（2026-09-06）：隔离当前源码后端实例 + seed 登录身份，auth/me → 历史 → 明细 → CSV/ZIP 实际落盘且内容与 DB 记录一致；对象存储 SDK 层按 `test_maps_online_real.py` 同款机制本地化。**外部 R2/AliOSS 未验证；用户决定停止验收**）
- [x] 仅本轮文档改动时检查相对链接、Markdown 结构和内容一致性，不构建、不启动、不运行应用测试。

## 验收记录

- 规划阶段：源码调用链、Astro/Vue 官方集成与依赖兼容性已核对；Vue 方案已通过独立 reviewer 审查，返回“干的不错”。两份文档的相对链接检查通过；现有样式检查已覆盖 Vue 文件。
- 实施（2026-09-06，worker ccglm / reviewer astra）：
  - `pnpm build`（astro check + vue-tsc --noEmit + astro build）通过；27 页输出，sitemap 19 URL（dashboard 三路径已排除），dashboard 产物 `noindex, nofollow` 且无私人数据。
  - 聚焦 e2e：dashboard.spec **6 用例 ×（chromium + Mobile Chrome）= 12/12 全过**（含 401 重登恢复原视图、Google 本站回跳换票与补邮箱验证保留 Dashboard 目的地）；website.spec Pricing 主路径与站点结构 + landing-pages.spec **6/6 通过**。含 reviewer 第一轮 7 项与第二轮 2 项修复后的聚焦回归。
  - `module-scripts.test.js`：3 处断言已适配（pricing copy 的 account/cancellationGuide 键改断言 dashboard 字典、initPricingPage 源码锚 restoreUser→restoreSignedInUser、dist 页面数 24→27），定向重跑 **3/3 通过**；`Pricing maps loader` 行为用例兼容通过。
  - `ui_token_lint.py`：本轮新增文件违规清零；存量 `ClinkReturnPage.astro` 4 处间距违规在 HEAD 基线已存在，裁决不修，全局命令保持失败（exit 1）。
  - 真实历史下载冒烟：初查时曾误判「未执行」（当时误查单数表名 `maps_online_task`、并据此误提「需同步 schema」，均已撤回）；最终以隔离实例完成，见下方「真实集成验收补充」。环境更正事实：Online 任务表为 `maps_online_tasks`（复数）+ 零填充分表 `maps_online_task_items_{00..19}`；`user_subscriptions` 列为 `product_kind`（新结构）；7600 旧进程的 `/auth/me` 500 系运行进程为旧代码（与库不匹配），非 schema 问题。
- review 状态：**三轮已通过**（reviewer astra：第一轮 7 项阻塞、第二轮 2 项修复均闭环，第三轮返回「干的不错」，无未解决代码阻塞）。
- 本地存储替身下载闭环（2026-09-06）：隔离当前源码后端（7601，自建进程）+ 专用网站实例（7745，`PUBLIC_API_BASE_URL` 指向 7601）+ seed 身份（user_id=3699）完成真实浏览器闭环：`auth/me` 200、真实 DB 列表 3 任务（processing 无 ZIP）、明细走真实分表、缺文件对象走真实 404 就地反馈、CSV 落盘 8918 bytes/121 行（BOM + 表头 + 120 数据行，与 record_count 一致）、ZIP 落盘 1818 bytes 服务端打包（unzip 3 个 CSV 与三 keyword record_count 一致）。对象存储 SDK 边界按 real 测试同款机制本地化；**外部 R2/AliOSS 真实端点与真实 Google OAuth 未验证**，为遗留外部验证项。
- 验收状态：**代码已落地、三轮 review 通过、本地后端+DB+本地存储替身下载闭环通过**；外部对象存储端点未验证，用户决定停止验收。
- 已裁决不修：存量 `ClinkReturnPage.astro` 4 处 lint 违规（HEAD 基线遗留）；全局 `ui_token_lint.py` 命令保持失败（exit 1），本轮改动文件零违规。
