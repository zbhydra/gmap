# 用户 Dashboard 实施清单

> 待用户批准，未开始实施。目标与边界见 [Dashboard 方案](../tech-用户Dashboard.md)。

## 1. 全站认证与工作区骨架

- [ ] 在 `website/package.json`、`pnpm-lock.yaml`、`astro.config.mjs` 接入兼容 Astro 5 的 Vue 官方集成及 Vue 类型检查；同步 `docs/references/specs/spec-website.md` 与 `website/README.md` 的组件、状态和构建边界。
- [ ] 核对登录控制器、事件、全局对象及 Pricing 待购引用；将 `PricingAuthModal.astro`、`pricing-auth-controller.ts` 的公共能力迁到 `components/auth/`，修改 Pricing 必要调用方，删除旧包装。
- [ ] 对本轮触及的现有界面按方案的迁移判据处理；确需 Vue 重构时连同必要调用方一起迁移，删除旧 DOM 控制器，复用已有验证路径，不扩大业务功能。
- [ ] 修改 `website/src/layouts/Layout.astro`，接入导航登录态和工作区模式；在 `components/dashboard/` 增加 Vue 工作区根组件、侧栏和业务视图，在 `pages/dashboard/` 及语言路由以 `client:load` 装配三个页面；只提取本轮涉及的站级 Astro 组件。
- [ ] 接入个人信息、退出、直接访问认证和移动端菜单；保留插件登录桥独立流程。
- [ ] 在 `website/src/i18n/schema.ts` 与现有字典加入文案，在 `src/sitemap/languageSitemap.mjs` 排除工作区 URL。

## 2. 接入现有业务

依赖第 1 阶段。

- [ ] 在 Dashboard 历史页面接现有列表、详情和下载接口，按方案处理分页、空态、错误与手动刷新；二进制下载复用 API 请求头边界。
- [ ] 在订阅页消费三线摘要，将 `PricingPageShell.astro`、`pricing-page-controller.ts` 的渠道管理及 `PricingCancellationGuideModal.astro` 迁入订阅管理；Pricing 保留选购和升级能力。
- [ ] API 管理展示未开放状态；核对 `components/pages/LandingPage.astro` 与营销内容配置中获取 Key 类 CTA，统一指向 API 管理页。
- [ ] 同步 `015/feat.md` 的入口归属、`011/tech-pricing与自动续费.md` 的账户管理归属及相关 i18n 文案；ROADMAP 只登记实际交付状态。保持首页采集入口原范围。

## 3. 最少验证

- [ ] `cd website && pnpm build`，确认包括 Vue 单文件组件类型检查，随后 `pnpm dev -- --host 127.0.0.1 --port <空闲端口>` 启动检查；运行 `python3 scripts/ui_token_lint.py` 检查本轮样式，并核对它覆盖新增 Vue 文件。
- [ ] 更新 `website/e2e/website.spec.ts` 的现有 Pricing 购买路径，覆盖登录后继续购买、升级与迁移后的渠道管理；只运行对应测试，不跑整个套件。
- [ ] 一组聚焦 Dashboard 的 Playwright 用例覆盖：匿名登录与取消、登录后导航与直达子页、退出回主页；历史分页与明细、CSV/ZIP 下载及无文件反馈；三线免费/有效/不可用状态、Pricing tab 跳转与 API 未开放。复用浏览器身份注册规则。
- [ ] 桌面及移动视口各检查一次截图、菜单与个人信息可达、长邮箱换行、焦点和无重叠。补充检查工作区 HTML 无私人数据且 noindex、sitemap 不收录。
- [ ] 用已有真实登录态和 Online 任务完成一次历史查询与下载冒烟；无账号、任务或存储凭据时记录未执行及原因，不以 route mock 冒充真实集成通过。第三方真实登录与支付证据按既有合同复用，新增变更涉及的回跳需验证。
- [ ] 仅本轮文档改动时检查相对链接、Markdown 结构和内容一致性，不构建、不启动、不运行应用测试。

## 验收记录

- 规划阶段：源码调用链、Astro/Vue 官方集成与依赖兼容性已核对；Vue 方案已通过独立 reviewer 审查，返回“干的不错”。两份文档的相对链接检查通过；现有样式检查已覆盖 Vue 文件。
- 实施、构建、启动、浏览器及真实接口验证：未执行。
