# 002 · 营销站 Online 与 API 落地页(执行计划)

> 需求依据:`@../feat.md` D5 附(2026-08-31 hydra 裁决:**占位入口全部接真落地页,页面可打开,页内未落地功能按钮点击无效**;替代 2026-08-30「Online 入口留空点击无效」基线)。范围拍板:对标全量 5 页(hydra 2026-08-31)。
> 竞品形态:gmapsextractor.com 导航(2026-08-31 抓取:API 下拉 4 项 `/google-maps-scraper-api`、`/google-maps-reviews-scraper-api`、`/google-maps-photos-api`、`/google-maps-scraper-mcp`;Online Scraper 为 pricing tab 形态)。
> 工程落点:`website/`(2026-08-31 起由原 website-mapsgrab/ 更名,dev 端口 7620)。
> 状态机:`pending → running → review → fix → done`;`blocked` = 需用户决策。

## 0 · 方案要点

- **新增落地页 ×5**(EN 基线,root + `[lang]` 双份,同既有页面模式):
  `/online-scraper/`、`/google-maps-scraper-api/`、`/google-maps-reviews-scraper-api/`、`/google-maps-photos-api/`、`/google-maps-scraper-mcp/`。
  Online 采用独立落地页,不做 Pricing tab 重构(本站 Pricing 三档为插件档,竞品 tab 形态不适用)。
- **导航接线**(`Layout.astro`):Online 占位 span → 真链接;新增 API 桌面下拉(4 子项,交互复用语言切换下拉既有模式);移动端 `mobile-nav` 平铺(Online + API 4 子项)。
- **首页产品卡接线**(`HomePage.astro`):Online/API 占位卡(aria-disabled 无链接)→ 链接卡(Online → `/online-scraper/`,API → `/google-maps-scraper-api/`),与插件卡同交互语义。
- **无效按钮语义**:落地页内未落地功能 CTA(Online 的「Start scraping」类、API 页的「Get API key」类)渲染为 `<button type="button" aria-disabled="true">`,无 JS handler——点击无跳转、无请求、无报错;不埋点。014 云端落地时替换为真交互。
- **页面内容**:以竞品对应页公开口径改写为 MapsGrab 事实(同 W3 法务页模式),不虚构用户数据/评价;素材:`@../references/D2-文章矩阵与产品矩阵.md` + 竞品页抓取。不加「Coming soon」类徽标。
- **同步**:`i18n/schema.ts` + `i18n/lang/en-US.ts` 文案、sitemap / llms.txt / robots(009 口径,新 10 路由入内)、canonical/hreflang 走既有机制、e2e spec。

## 1 · 执行单元

| # | 单元 | 范围 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| U1 | 落地页 ×5 + 导航/卡片接线 | §0 全部。契约文件(Layout、i18n、sitemap)仅本单元触碰,无并行互斥问题 | plan 001 W1–W7 全 done | ✅ done(2026-08-31,worker-reviewer 三轮闭环:R1 2 finding(LandingPage 死 `t`、pageBySlug 字典复制 ×10)修复;R2 复验 F1 残留(getContent import 未删,worker 首轮验证汇报失实已纠正)再修;R3 终验「干的不错」。验证:build 23 页 0 err/0 warn、e2e landing-pages+website.spec 全绿(唯一失败 = website.spec:245 Pricing 用例,归并行 Pricing tab 重构会话)、Lighthouse 新页 100×4、type-check:tests 通过。落点:5 页 + Layout 导航/API 下拉/移动平铺、LandingPage.astro 共用模板、i18n/landing.ts 映射收敛、10 路由薄壳化、占位语义(status/comingSoonLabel/is-coming-soon/.nav-link-placeholder)删净) |

### U1 验收

1. `pnpm build`(含 astro check)通过;新增路由 ×10(5 页 × root/`[lang]`)。
2. e2e 全绿且全自动:5 页可打开;导航 Online/API(含下拉展开与子项跳转)可点;首页两卡跳转正确;无效按钮点击后 URL 不变、无网络请求;既有 spec 无回归。sitemap 一致性由构建契约兜底(新路由必须同步 `src/sitemap/languageSitemap.mjs` 映射,否则构建 throw);llms.txt 为手写静态文件,按 009 口径构建后人工核对新路由入列,不进 e2e。
3. Lighthouse 新页 ≥95(perf/a11y/seo)。
4. 零新增第三方运行时依赖;UI 消费 design token;文案 i18n;TS 类型边界按 spec-website §2。

## 2 · 非目标

- 014 B1(Online Scraper)/B2(三 API)/B3(MCP)功能本体与按钮后端接线。
- Guides/对比文章内容线(阶段 4 运营)。
- Pricing 页 Online/API 卡现状(占位不可购,维持)。
- 多语言扩展(EN 基线不变)。

## 3 · 风险与已接受取舍

- API 子页内容在 014 B2 契约定稿前按竞品公开口径编写,契约定稿后需内容修订——已接受(hydra 拍板全量 5 页,换取菜单形态即刻对齐 + 提前收录)。
- MCP 页受众为开发者,文案口径与插件页不同,SEO 关键词按竞品 slug 对齐。

## 4 · 状态日志

- 2026-08-31 计划创建(hydra-feat)。U1 pending。
- 2026-08-31 U1 done(execute-loop):单 worker + 单 reviewer 三轮(实现 → 2 finding 修复 → F1 残留复修 → 终验「干的不错」)。验证:build 23 页 / e2e 本单元全绿 / Lighthouse 新页 100×4 / 零新增依赖。范围外已证实问题反馈:Pricing 页 e2e 用例因并行会话 Pricing tab 重构在途而失败,归该会话收敛;`toolDom.ts:44` execCommand deprecation hint 为既有。
